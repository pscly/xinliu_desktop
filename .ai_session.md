## [2026-03-01 18:26] - 任务：Task 41 冲突中心（preload + renderer + tests）

- 原始需求：打通冲突中心到 renderer，冲突路由不再使用 demo 数据；展示 Flow rejected/server_snapshot 与 Notes 冲突副本，并提供 Flow 三种裁决动作（force_overwrite 二次确认），且动作后刷新列表。
- 实现目标/逻辑：
  - preload 暴露 `window.xinliu.conflicts` 用例级 API，全部复用 `invokeIpc` 返回 `IpcResult`。
  - renderer `conflicts` 路由改为真实冲突中心组件，支持 Flow/Notes 双列表、可解释错误展示、preload 未注入降级提示与无崩溃交互。
  - force_overwrite 不用 `window.confirm`，改为组件内确认面板（state 驱动），确保可测稳定。
  - 单测新增冲突中心最小渲染与关键交互（apply_server 调用 + force_overwrite 确认流程）。
- 核心变更：
  - `src/preload/index.ts`
  - `src/renderer/vite-env.d.ts`
  - `src/renderer/App.tsx`
  - `src/renderer/App.test.tsx`
  - `.sisyphus/notepads/xinliu-desktop/learnings.md`（追加 testid 与刷新策略）
- 遗留/约束：
  - 仅完成 Task 41 范围，不扩展到同步调度器/设置页后端配置等非本任务内容。

## 2026-02-24 18:34 - 任务：补全 xinliu_desktop 桌面端设计到可落地规格

- 原始需求：先不着急开发，先详细检查现有设计文档哪些地方需要补全，并把补全内容整理进 plan 文档。
- 实现目标/逻辑：
  - 以仓库根目录 `PLAN.md` 作为“桌面端主规格文档”落点，按章节补齐：IA/Triptych、Windows 系统集成、混合后端合同对照（Flow + 直连 Memos）、本地 SQLite 数据模型、同步与冲突策略、安全边界、更新发布、验收清单。
  - 使用 `.sisyphus/plans/desktop-design-completion-plan.md` 作为执行计划；采用“分章节草稿并行产出 → 合并到 `PLAN.md`”的方式降低冲突。
- 核心变更：
  - 新增计划：`.sisyphus/plans/desktop-design-completion-plan.md`
  - 启动 boulder：`.sisyphus/boulder.json`
  - 初始化 notepad：`.sisyphus/notepads/desktop-design-completion-plan/*`
- 已确认关键决策：
  - 主规格文档落点：更新仓库根目录 `PLAN.md`
  - 后端：混合模式（Flow 负责登录+Todo/Collections/Sync；Notes 直连 Memos）
  - IA：Folder 树为主
  - Todo：完整 RRULE（对齐 occurrences 模型）
- 遗留/约束：
  - Windows-only（不承诺 macOS/Linux）；必须去掉原生丑标题栏；不做“手机放大版”。
  - 所有同步/并发语义以 `apidocs/*` 与 Memos 官方 API 为准，不自创协议字段。

## 2026-02-25 02:54 - 任务：输出设计规格文档骨架（目录 + 术语 + 风格）

- 原始需求：产出可合并进根目录 `PLAN.md` 的目录骨架，同时补齐关键术语表、来源映射、写作规范。
- 实现目标/逻辑：
  - 以“Windows-only + Triptych 三栏基线 + Folder 树 IA + 混合后端（Flow + 直连 Memos）”作为写作前提，给出一级/二级目录，覆盖目标与非目标、IA/导航、窗口与系统集成、数据模型、同步与冲突、安全模型、错误处理、性能预算、安装与更新、验收清单。
  - 术语表强制对齐 `apidocs/api.zh-CN.md`、`apidocs/collections.zh-CN.md` 的合同字段与语义，包括 `client_updated_at_ms`、`server_snapshot`、tombstone/`deleted_at`、`sync/pull`、`sync/push`、`applied`、`rejected`、`collection_item(s)`、`memoName`（`memos/123`）等。
  - 若存在信息缺口，禁止使用 `TBD/待定/未决`，统一写成“已知缺口 + 需要补齐的证据来源类型”。
- 核心变更：
  - 新增草稿：`.sisyphus/drafts/plan-sections/02-outline.md`
  - 追加记录：`.sisyphus/notepads/desktop-design-completion-plan/learnings.md`
- 遗留/约束：
  - 本任务不修改根目录 `PLAN.md`，仅产出可合并草稿，供后续任务合并。

## [2026-02-25 03:30] - 任务：将桌面端主规格合并进根目录 PLAN.md（唯一规格）

- 原始需求：先补全桌面端设计到可落地实现，再把分散内容统一整理成可执行的主规格文档。
- 实现目标/逻辑：
  - 采用“分章节草稿并行产出 → 最终合并到根目录 `PLAN.md`”的工作流，降低冲突并保证 `PLAN.md` 成为唯一桌面端主规格。
  - 明确写作/实现前提：Windows-only；不做“手机放大版”；采用 Triptych（三栏）作为桌面信息架构基线；去掉原生丑标题栏。
  - 后端采用混合模式：Flow 负责登录与 Todo/Collections/Sync；笔记直连 Memos（合同字段与语义以 `apidocs/*` 与 Memos API 为准）。
  - 增加可验收的核对脚本/证据输出，确保主规格合并后可被机器化检查（`final-plan-verify.txt` 标记 PASS）。
- 核心变更：
  - 主规格合并：`PLAN.md`（成为唯一桌面端主规格）
  - 入口指针：`DESIGN.md` 顶部指向 `PLAN.md`
  - 证据落盘：`.sisyphus/evidence/*`（含 F1-F4 与 `.sisyphus/evidence/final-plan-verify.txt` PASS）
  - 草稿迁移：将分章节草稿迁移到 `.sisyphus/evidence/design-drafts/plan-sections/`
- 遗留/约束：
- 当前仓库仍缺少可直接执行的 build/test 脚本（验收更多依赖文档核对与人工/后续脚本补齐）。
- LSP 尚未配置 marksman（Markdown 诊断/跳转能力受限）。
- 已删除 `.sisyphus/drafts/design-doc-audit.md`，是否需要恢复/保留为长期审计清单仍待决定。

## 2026-02-25 06:55 - 任务：修订 PLAN.md（Notes 混合模式 + 可降级，消除歧义）

- 原始需求：执行 `.sisyphus/plans/plan-md-fixups-hybrid-fallback.md`，把 `PLAN.md` 修到“零歧义、零自相矛盾、可直接指导实现”。
- 实现目标/逻辑：
  - 明确契约优先级与 `collection_item` 漂移裁决（专题合同优先）。
  - 写清 Notes Provider 路由决策树：默认直连 Memos，失败时降级到 Flow Notes（401/403/超时/BaseURL 不合法等）。
  - 写清 Source-of-Truth（SoT）与“禁止隐式双写”护栏，并给出 Ownership Matrix。
  - 消除 “Flow 不负责 Notes” 与 “Flow 侧必须落库 notes” 的矛盾：保留 `notes` 仅作为降级承载/离线镜像，禁止默认路径走 Flow `resource=note`。
  - 更新 Mermaid 时序图展示 Notes 降级路径；扩展验收清单与一键总判定，保证可执行并输出 PASS。
- 核心变更：
  - 规格修订：`PLAN.md`
  - 计划追踪：`.sisyphus/plans/plan-md-fixups-hybrid-fallback.md`
  - 证据输出：`.sisyphus/evidence/task-1-contract-precedence.txt` 至 `.sisyphus/evidence/task-8-final-verify.txt`
  - 过程记录：`.sisyphus/notepads/plan-md-fixups-hybrid-fallback/learnings.md`
- 遗留/约束：
  - 仓库根目录未提供 `bun run build` 脚本，`bun run build` 会报 `Script not found "build"`；`bun test` 当前无测试文件匹配（与本计划“仅文档修订”一致，但需后续工程化补齐）。

## 2026-02-25 11:09 - 任务：开始实现心流 Windows 桌面端（从 0 落地工程与全功能）

- 原始需求：设计已基本完成，开始开发；不做 MVP，要完整完善版本。
- 实现目标/逻辑：
  - 以 `.sisyphus/plans/xinliu-desktop.md` 作为执行计划，按任务索引顺序推进（Wave 1→5）。
  - 以根目录 `PLAN.md` 作为 Windows 行为与验收合同；接口字段与同步合同以 `apidocs/*` 更具体/更新/专题者优先。
  - 默认技术选型：Electron + React + TypeScript；SQLite(better-sqlite3) 仅 main；TDD（Vitest + Playwright）；NSIS 安装包（electron-builder）+ GitHub Releases stable 自动更新（electron-updater）。
- 已确认关键决策：
  - 产品名：心流；英文标识：xinliu-desktop
  - appId：cc.pscly.xinliu.desktop（必须稳定，影响通知归属与更新一致性）
  - 后端：Flow 默认 https://xl.pscly.cc；混合模式（Flow=Auth/Todo/Collections/Sync；Notes 直连 Memos + 降级路由）
  - 发布：仅 stable；GitHub 仓库 pscly/xinliu_desktop；当前无 Releases
  - 代码签名：暂无证书；允许未签名用于内测/自用（计划中保留 CI secrets 注入签名的落点与风险说明）
- 核心变更：
  - 新增执行计划：`.sisyphus/plans/xinliu-desktop.md`
  - 启动 boulder：`.sisyphus/boulder.json`（active_plan 指向 xinliu-desktop）
  - 初始化 notepad：`.sisyphus/notepads/xinliu-desktop/*`
- 遗留/约束：
  - 当前仓库还没有 `package.json`/`src`；需从任务 1 开始搭工程骨架并建立 CI/测试基建。
  - Windows-only；frameless；renderer 禁 Node/SQLite/任意路径 IO；token 不落明文不写日志。

## [2026-02-25 11:56] - 任务：初始化工程骨架（Electron + Vite + React + TypeScript）

- 原始需求：在仓库根目录从 0 搭建工程骨架；目录结构固定为 `src/main`、`src/preload`、`src/renderer`、`src/shared`；主窗口必须 `frame: false`；renderer 禁用 Node（不得开启 `nodeIntegration`）。
- 实现目标/逻辑：
  - 使用 Vite 构建 renderer（输出到 `dist/renderer/`），使用 `tsc` 编译 main/preload（输出到 `dist/`）。
  - `npm run typecheck` 与 `npm run build` 只做编译/构建，不启动 Electron GUI；验证命令 `npm ci` / `npm run typecheck` / `npm run build` 需在 Linux 退出码 0。
  - `vite.config.ts` 仍保持 `.ts`：通过将根 `tsconfig.json` 的解析选项设为 `moduleResolution=Bundler`，避免 TypeScript 按 CommonJS 条件分支解析 `vite` 导致 `defineConfig` 类型缺失。
- 核心变更：
  - 根目录脚手架：`package.json`、`package-lock.json`、`tsconfig.json`、`tsconfig.electron.json`、`tsconfig.renderer.json`、`vite.config.ts`、`index.html`、`.gitignore`。
  - 分层入口：`src/main/main.ts`（BrowserWindow: `frame:false`、`nodeIntegration:false`、`contextIsolation:true`）、`src/preload/index.ts`（`contextBridge` 暴露只读版本信息）、`src/renderer/*`（React 最小 UI）、`src/shared/appMeta.ts`。
  - LSP 兼容：新增 `src/main/tsconfig.json`、`src/preload/tsconfig.json`、`src/renderer/tsconfig.json` 以让 TS 语言服务按层读取正确的编译选项。
- 遗留/约束：
  - 仅完成“工程骨架闭环”，未实现托盘常驻/关闭到托盘/全局快捷键等后续合同功能（由后续任务完成）。
  - 当前仓库存在 `.npmrc` 的 `electron_mirror/electron-mirror` 配置警告；不影响构建通过，但未来 npm 大版本可能需要清理/迁移该配置。

## [2026-02-25 10:15] - 任务：调研 Windows Runner 上跑 Electron + Playwright E2E（GitHub Actions）

- 原始需求：研究如何在 GitHub Actions 的 Windows runner 上稳定运行 Playwright 驱动的 Electron E2E，并能产出可调试的 artifacts。
- 实现目标/逻辑：
  - 采用 Playwright 的 `_electron.launch({ args/executablePath/env })` 路径；测试定位策略以 `data-testid` 为硬契约；CI 里统一上传 `playwright-report/` 与 `test-results/`。
  - 明确 Windows 特性风险：窗口 focus 行为受系统限制；Electron 进程可能因 tray/后台常驻或 IPC 子进程泄漏导致 teardown 卡死，必要时准备“强制杀进程兜底”。
- 核心变更：
  - 追加记录：`.sisyphus/notepads/xinliu-desktop/learnings.md`
- 遗留/约束：
  - 需要在后续实现任务中加入“测试模式”开关（禁托盘/禁自动更新/禁后台常驻）来提升 CI 稳定性。

## [2026-02-25 12:20] - 任务：建立代码规范 + TDD 测试基建（TypeScript/ESLint/Prettier/Vitest）

- 原始需求：建立桌面端的代码规范与测试基建，要求 `npm run lint/test/typecheck` 在本机 Linux 全部退出码 0；至少包含 2 个测试（shared 纯函数单测 + renderer 最小渲染测试）。
- 实现目标/逻辑：
  - ESLint 与 Prettier 分工：ESLint 管正确性，Prettier 管格式（通过 `eslint-config-prettier` 关闭冲突规则）。
  - ESLint 采用 v9 Flat Config（`eslint.config.mjs`），避免 `.eslintrc.*` 在 ESLint v9 下失效导致 CI 报错。
  - Vitest 版本与 Vite 主版本对齐：当前工程使用 Vite v5，因此将 Vitest 固定为 `vitest@2.1.9`，避免 Vitest(v4+) 引入 Vite v7 造成类型重复与 LSP/TS 报错。
  - 测试保持离线与确定性：不访问外网/后端；renderer 测试用 jsdom + Testing Library，仅验证组件可渲染与关键文案存在。
- 核心变更：
  - scripts：`package.json` 新增 `lint`、`format`、`test`，保留原有 `typecheck`、`build`。
  - ESLint：新增 `eslint.config.mjs`。
  - Prettier：新增 `.prettierrc.json`、`.prettierignore`。
  - Vitest：新增 `vitest.config.ts`。
  - 测试与 shared 纯函数：新增 `src/shared/number.ts`、`src/shared/number.test.ts`、`src/renderer/App.test.tsx`。
- 遗留/约束：
  - `npm` 仍可能输出 `.npmrc` 的 `electron_mirror` unknown config 警告（不影响本任务通过）。
  - `vitest` 运行时可能输出 Vite Node API CJS deprecate 警告（不影响本任务通过）。

## [2026-02-25 12:21] - 任务：建立 GitHub Actions CI（Windows runner：lint/test/build）

- 原始需求：GitHub Actions 在 PR / push 时在 `windows-latest` 运行并依次执行：`npm ci`、`npm run lint`、`npm run test`、`npm run typecheck`、`npm run build`；并上传基础 artifacts（`.sisyphus/evidence/` 与 `dist/`，目录不存在也不应失败）。
- 实现目标/逻辑：
  - 新增工作流 `.github/workflows/ci.yml`，触发事件为 `push` 与 `pull_request`。
  - runner 固定为 `windows-latest`，Node 固定为 20，并启用 setup-node 的 npm 缓存以提升稳定性与速度。
  - 每个命令单独一个 step，确保 CI 日志可定位失败点。
  - artifacts 使用 `actions/upload-artifact@v4`，并设置 `if-no-files-found: ignore`，避免目录缺失导致失败。
- 核心变更：
  - 新增：`.github/workflows/ci.yml`
- 遗留/约束：
  - 本任务不引入 release/发布逻辑；仅提供 Windows runner 的 lint/test/typecheck/build 基线。
  - `.npmrc` 中的 `electron_mirror` 可能在 npm 输出 unknown config 警告，但不影响 `npm ci` 与后续脚本执行。

## 2026-02-25 12:33 - 任务：Electron 安全基线（BrowserWindow/WebContents/导航拦截）

- 原始需求：集中封装 BrowserWindow 安全默认值（显式硬写死）并在 main 进程安装导航拦截；新增可在 Node/Vitest 环境运行的单测。
- 实现目标/逻辑：
  - 新增 `src/main/security.ts`，提供纯函数 `buildSecureWebPreferences/buildSecureBrowserWindowOptions`，强制覆盖 `contextIsolation/nodeIntegration/webSecurity/allowRunningInsecureContent` 这四项 MUST 值。
  - 提供 `installNavigationGuards(webContents, { openExternal })`，通过依赖注入避免测试里导入 electron runtime；白名单仅允许 `file:` 导航，其余 scheme 一律拦截；对 http(s) 外链使用系统浏览器打开。
  - Vitest 全局为 jsdom，因此 main-side 单测文件用 `// @vitest-environment node` 覆盖为 Node 环境，避免 DOM 依赖。
- 核心变更：
  - 修改：`src/main/main.ts`（使用集中安全默认值，并对主窗口 webContents 安装导航拦截）
  - 新增：`src/main/security.ts`
  - 新增：`src/main/security.test.ts`
- 遗留/约束：
  - Node/Vitest 环境下不能 `import { app, BrowserWindow } from 'electron'`（electron 包在 Node 下并不提供 runtime API），因此测试必须只覆盖纯函数与依赖注入逻辑。

## 2026-02-25 12:54 - 任务：修复外链打开失败的错误处理（避免空 catch）

- 原始需求：修正代码质量硬约束：避免空 `catch {}`；外链打开失败需可观测但不得向 renderer 泄漏内部堆栈/路径。
- 实现目标/逻辑：
  - 在 `installNavigationGuards` 内新增 `openExternalSafely`：同步调用 `openExternal(url)`，并对 Promise reject 与同步 throw 都记录告警日志（仅输出 `String(error)`）。
  - 保持行为不变：仍默认 deny 导航与 `window.open`，仅对 http(s) 外链尝试交给系统浏览器打开。
- 核心变更：
  - 修改：`src/main/security.ts`
- 遗留/约束：
  - 日志体系（脱敏落盘、诊断面板）将在后续任务单独实现；此处仅提供最小可观测 hook。

## 2026-02-25 13:05 - 任务：IPC 桥与 Preload 合同（白名单 + 参数校验 + 统一错误形状）

- 原始需求：定义“静态可枚举”IPC 白名单；preload 仅暴露用例级 API；main 侧参数校验+限流；统一错误结构，禁止把堆栈/绝对路径透传给 renderer。
- 实现目标/逻辑：
  - 在 shared 层新增 IPC 常量与通用返回结构：`src/shared/ipc.ts`（静态可枚举，后续扩展集中在此）。
  - main 侧新增 `registerIpcHandlers(ipcMain, deps)`：显式逐一注册 `ipcMain.handle`，并通过依赖注入 `getWindowForSender` 避免在 Node/Vitest 下导入 electron runtime。
  - 参数校验：对当前窗口控制 IPC 统一要求空对象 payload（非空一律 `VALIDATION_ERROR`）。
  - 限流：为每个 channel 提供简单的固定时间窗限流（默认 1s/60 次），超限返回 `RATE_LIMITED`。
  - preload 侧新增 `invokeIpc` 包装：捕获 `ipcRenderer.invoke` 的异常并返回 `IpcResult`，避免把异常对象/堆栈直接暴露给 renderer。
- 核心变更：
  - 新增：`src/shared/ipc.ts`
  - 新增：`src/main/ipc.ts`、`src/main/ipc.test.ts`
  - 修改：`src/main/main.ts`（注册 IPC handlers）
  - 修改：`src/preload/index.ts`（暴露 `window.xinliu.window.*` 用例级 API）
  - 修改：`src/renderer/vite-env.d.ts`（补齐 window.xinliu 类型）
  - 证据：`.sisyphus/evidence/task-05-ipc-guard.txt`、`.sisyphus/evidence/task-05-ipc-validate.txt`
- 遗留/约束：
- 目前 IPC 仅覆盖“窗口控制”这一组（为 Task 6 自定义标题栏做准备）；后续系统能力（托盘/快捷键/存储迁移/更新/协议）会按同一模式逐一扩展白名单。

## 2026-02-27 15:12 - 任务：Task 30 自动更新（GitHub Releases stable：检查/下载/延后安装/失败回退）

- 原始需求：启动后轻量检查更新；设置页提供手动检查按钮（data-testid="check-updates"）；后台下载，下载完成后由用户触发安装；支持“稍后再说/延后”；失败可见、可重试，并提供“打开 Releases”退路；stable only。
- 实现目标/逻辑：
  - main 侧实现可依赖注入的 updater 状态机（UpdaterController + AutoUpdaterAdapter），单测在 Vitest(node) 运行，禁止直接 import Electron runtime 或 electron-updater 副作用对象。
  - 运行期用 electron-updater 适配器接线；强制 allowPrerelease=false + autoDownload=false + autoInstallOnAppQuit=false，并在 update-available 后后台触发 downloadUpdate。
  - IPC 合同静态可枚举：新增 IPC_CHANNELS.updater + IPC_EVENTS.updater.statusChanged；main 注册 handlers + renderer 通过 preload 用例级 API 调用/订阅。
  - renderer 设置页新增自动更新区块：显示状态、提供“检查更新/安装并重启/稍后再说/重试/复制错误/打开 Releases”。
- 核心变更：
  - main：src/main/updater/\*、src/main/main.ts、src/main/ipc.ts、src/main/ipc.test.ts
  - shared：src/shared/ipc.ts（updater 通道与事件 + UpdaterStatus）
  - preload：src/preload/index.ts（window.xinliu.updater）
  - renderer：src/renderer/App.tsx、src/renderer/App.test.tsx、src/renderer/vite-env.d.ts
  - e2e：playwright.config.ts、e2e/updater.spec.ts
  - evidence：.sisyphus/evidence/task-30-updater-unit.txt、.sisyphus/evidence/task-30-updater-e2e.png
- 遗留/约束：
  - 开发模式（非安装包）下自动更新会回退为可解释状态；真实更新下载/安装需在打包产物中验证。

## 2026-02-25 13:15 - 任务：Task 6（Frameless 主窗口 UI 壳：自定义标题栏 + Triptych 三栏布局 + 路由骨架）

- 原始需求：在 renderer 侧实现 frameless 主窗口 UI 壳，包含自定义标题栏（drag/no-drag）、Triptych 三栏布局骨架与最小路由占位，并补齐最小渲染测试与证据输出（不修改 main/preload/ipc，不新增依赖）。
- 实现目标/逻辑：
  - 标题栏：使用 `-webkit-app-region: drag` 实现可拖拽区域；交互控件使用 `-webkit-app-region: no-drag`；窗口按钮通过 `window.xinliu.window.{minimize,toggleMaximize,close}` 调用，并对 `IpcResult` 失败做最小处理（仅 `console.warn` error.code，不 throw）。
  - Triptych：renderer 主体为三栏网格（左/中/右），并为三栏与导航入口提供硬契约 `data-testid`，确保可被测试与后续 E2E 定位。
  - 路由骨架：不引入 react-router，使用 `useState` 在 `Notes/Collections/Todo/设置/冲突` 五个入口之间切换；中栏/右栏提供非空占位内容，避免“手机放大版”的单列空白。
  - 测试：更新 renderer 单测，断言标题栏按钮、Triptych 三栏与导航 testid 存在，并验证在无 preload 注入（无 Electron）时点击窗口控件不会导致渲染崩溃。
  - 证据：输出 `.sisyphus/evidence/task-06-shell.txt` 与 `.sisyphus/evidence/task-06-routes.txt`，包含 rg 与 `npm run test` 输出。
- 核心变更：
  - 修改：`src/renderer/App.tsx`（Titlebar + Triptych + 最小路由骨架）
  - 修改：`src/renderer/styles.css`（标题栏 drag/no-drag + 三栏布局与占位样式）
  - 修改：`src/renderer/App.test.tsx`（最小渲染 + 关键 data-testid 断言）
  - 新增：`.sisyphus/evidence/task-06-shell.txt`、`.sisyphus/evidence/task-06-routes.txt`
- 遗留/约束：
- 当前仅提供“UI 壳与路由占位”，Notes/Collections/Todo/设置/冲突的真实功能实现将在后续任务落地。
- 单测里点击标题栏按钮会产生 `console.warn`（因为 jsdom 无 preload 注入），属预期降级行为。

## 2026-02-25 13:40 - 任务：完成 Task 7（Storage Root layout + relpath）并开始 Task 8（SQLite 连接 + 迁移引擎）

- 原始需求：
  - Task 7：落地 Storage Root 目录布局与 relpath/absPath 转换规则，确保后续“更换数据目录”可迁移。
  - Task 8：引入 SQLite（better-sqlite3）main-only + WAL，并提供版本化迁移引擎（可在 Vitest node 环境测试）。
- 实现目标/逻辑：
  - Task 7：
    - 目录布局写死为 `<root>/db|attachments|attachments-cache|logs|tmp|exports`（对齐 `PLAN.md:1.5.2`）。
    - 所有“持久化路径字段”使用 relpath（POSIX `/` 分隔），确保 Storage Root 迁移只需切换 root。
    - relpath 安全校验：禁止绝对路径、禁止盘符、禁止反斜杠、禁止 `.`/`..`、禁止空路径段、拒绝指向 root 本身/越界。
  - Task 8：
    - 仅 main 进程使用 SQLite；renderer 不直连。
    - `openSqliteDatabase` 在打开连接后显式设置 PRAGMA：`journal_mode=WAL`、`foreign_keys=ON`、`synchronous=NORMAL`、`busy_timeout`。
    - 迁移引擎使用 `PRAGMA user_version` 作为版本号；迁移版本必须从 1 连续递增；在单事务内依次 apply 并推进版本。
    - 单测强制 `// @vitest-environment node`，避免导入 Electron runtime。
- 核心变更：
  - Task 7：
    - 新增：`src/main/storageLayout.ts`、`src/main/storageLayout.test.ts`
    - 更新：`.sisyphus/evidence/task-07-layout.txt`、`.sisyphus/evidence/task-07-relpath.txt`
  - Task 8：
    - 新增：`src/main/db/*`（paths/sqlite/migrations/index + 单测）
    - 新增证据：`.sisyphus/evidence/task-08-sqlite.txt`
    - 依赖：`better-sqlite3` + `@types/better-sqlite3`
- 已确认关键决策：
  - SQLite 文件名固定：`xinliu.sqlite3`（位于 `<root>/db/`）。
  - 迁移版本以 `user_version` 表示（0 表示未迁移）。
- 遗留/约束：
  - better-sqlite3 在某些环境可能需要本机编译工具链；CI/开发环境建议使用 Node 20（本仓库计划亦约束 Node 20 LTS）。

## 2026-02-25 13:46 - 任务：完成 Task 9（Flow 领域表 Schema：Todo/Collections + tombstone）

- 原始需求：在 SQLite 迁移中创建 Flow 必备表：`todo_lists`、`todo_items`、`todo_occurrences`、`collection_items`，并统一 tombstone 字段 `deleted_at`。
- 实现目标/逻辑：
  - 迁移版本 v2 新增上述 4 张表，并为常用查询添加索引（按 `parent_id`、`deleted_at`、`client_updated_at_ms` 等）。
  - 兼容离线优先与同步合同：字段与 apidocs/openapi 输出形态对齐（id/list_id/title/status/priority 等），并保留 `client_updated_at_ms`/`updated_at`/`deleted_at`。
  - 单测在 Node 环境执行迁移后，通过 `sqlite_master` 校验表存在，通过 `PRAGMA table_info` 校验 `deleted_at` 列存在。
- 核心变更：
  - 修改：`src/main/db/migrations.ts`（新增 v2：Flow 领域表）
  - 新增：`src/main/db/flowSchema.test.ts`
  - 证据：`.sisyphus/evidence/task-09-schema.txt`、`.sisyphus/evidence/task-09-collections-contract.txt`
- 遗留/约束：
  - 本任务仅落地 Schema，不包含具体读写服务与 outbox（后续任务 10/13+ 处理）。

## 2026-02-25 13:49 - 任务：开始 Task 10（outbox_mutations / sync_state / jobs / user_settings 表）

- 原始需求：在迁移中创建 `outbox_mutations`、`sync_state`、`jobs`、`user_settings` 表，为离线写入队列、同步 cursor、后台任务恢复与用户设置镜像提供持久化基础。
- 实现目标/逻辑：
  - 迁移版本 v3 新增上述 4 张表，并添加必要索引：
    - outbox 按 `status,next_retry_at_ms` 查询；按 `resource,entity_id` 定位；按 `client_updated_at_ms` 排序。
    - jobs 支持 `dedupe_key` 唯一约束与按 `status,next_run_at_ms` 调度查询。
  - user_settings 作为 Flow user_setting 镜像表，包含 `deleted_at` tombstone。
  - 单测：迁移后 sqlite_master 中存在 4 张表。
- 核心变更：
  - 修改：`src/main/db/migrations.ts`（新增 v3）
  - 新增：`src/main/db/metaSchema.test.ts`
  - 证据：`.sisyphus/evidence/task-10-schema.txt`
- 遗留/约束：
  - 本任务仅落地 Schema；outbox 状态机、cursor 更新与 jobs 执行器在后续任务实现。

## 2026-02-25 13:52 - 任务：固定本地 Node 版本约束（避免 native addon 编译失败）

- 原始需求：better-sqlite3 属于 native addon，本地若使用非 Node 20 LTS 可能无法稳定安装/编译。
- 实现目标/逻辑：
  - 增加 `.nvmrc` 固定为 `20.20.0`。
  - 在 `package.json` 增加 `engines.node: ">=20 <21"`，降低误用 Node 24+ 的概率。
- 核心变更：
  - 新增：`.nvmrc`
  - 修改：`package.json`

## 2026-02-25 13:56 - 任务：完成 Task 11（鉴权凭据 + 设备标识 + 脱敏护栏）

- 原始需求：token 安全持久化优先 keytar（Windows Credential Vault）；提供稳定 deviceId/deviceName；日志/诊断脱敏禁止 token/Authorization/绝对路径。
- 实现目标/逻辑：
  - TokenStore：优先使用 keytar；keytar 不可用时仅内存持有（确保不落盘/不写 SQLite）。
  - DeviceIdentity：通过可注入 KV store 持久化 `device_id`，并用 hostname 作为 deviceName（后续用于 Flow 请求头）。
  - Redaction：提供 `redactForLogs` 统一脱敏（Authorization Bearer、token/access_token、Windows/Posix 绝对路径）。
- 核心变更：
  - 新增：`src/main/auth/tokenStore.ts`、`src/main/device/deviceIdentity.ts`、`src/shared/redaction.ts`
  - 新增单测：`src/main/auth/tokenStore.test.ts`、`src/main/device/deviceIdentity.test.ts`、`src/shared/redaction.test.ts`
  - 证据：`.sisyphus/evidence/task-11-keytar.txt`、`.sisyphus/evidence/task-11-redaction.txt`
- 遗留/约束：
  - 目前尚未把 deviceId/deviceName 注入到 HTTP client（将在 Task 12 落地）。
  - keytar 作为 native addon 可能在非 Windows 环境不可用，因此提供内存 fallback；Windows 正式路径将在集成阶段验证。

## 2026-02-25 14:20 - 任务：完成 Task 12（HTTP client：Base URL 标准化 + request_id + ErrorResponse + 重试）

- 原始需求：实现统一 HTTP client 基础设施（Flow/Memos 各自实例可复用），包括 Base URL 标准化、每请求 request_id、非 2xx 优先解析 ErrorResponse，以及 401/429/5xx/网络失败的重试策略。
- 实现目标/逻辑：
  - Base URL 标准化：去尾部 `/`、确保 scheme（无 scheme 默认补 `https://`）、仅允许 http/https。
  - request_id：每次请求注入 `X-Request-Id`；并读取响应头 `X-Request-Id` 以便诊断。
  - ErrorResponse：对非 2xx best-effort 解析 `error/message/request_id/details`，未知字段容错。
  - 重试策略：
    - 401：不重试（交由上层触发重新登录）
    - 429：读取 `Retry-After`（秒）并等待后重试
    - 5xx/网络异常/超时：指数退避 + 抖动，最多 `maxAttempts`
- 核心变更：
  - 新增：`src/shared/url.ts`、`src/shared/url.test.ts`
  - 新增：`src/main/net/httpClient.ts`、`src/main/net/httpClient.test.ts`
  - 证据：`.sisyphus/evidence/task-12-http.txt`、`.sisyphus/evidence/task-12-request-id.txt`
- 遗留/约束：
  - 设备头 `X-Flow-Device-*` 与 Authorization 注入将在 Flow/Memos client 层落地（后续任务 16/34/42）。

## 2026-02-25 14:35 - 任务：完成 Task 13（Outbox 写入工具 + client_updated_at_ms 单调递增）

- 原始需求：提供 Flow outbox 写入基础能力，保证任何本地写入都能“同一事务写业务表 + outbox”；并对同一实体 id 做 `client_updated_at_ms` 单调递增（max(now_ms, last_ms + 1)）；delete 即使本地已 tombstone 也必须生成 op=delete outbox。
- 实现目标/逻辑：
  - 新增 outbox 工具模块：
    - bumpClientUpdatedAtMs：实现 max(now_ms, last_ms + 1)，覆盖 now_ms 回退场景。
    - enqueueFlowOutboxMutation：写入 outbox_mutations，默认 status=PENDING；op=delete 未提供 data 时写 data_json="{}"（满足 NOT NULL）。
    - withImmediateTransaction：基于 better-sqlite3 `db.transaction(...).immediate()`，用于保证业务表 + outbox 同步提交/回滚。
  - 单测覆盖：
    - bump 单调递增（含 now_ms 回退）。
    - 已 tombstone 仍生成 op=delete outbox。
    - 同一事务回滚：业务表写入 + outbox 入队在抛错时一起回滚。
- 核心变更：
  - 新增：`src/main/sync/outbox.ts`
  - 新增单测：`src/main/sync/outbox.test.ts`
  - 证据：`.sisyphus/evidence/task-13-bump.txt`、`.sisyphus/evidence/task-13-delete-outbox.txt`
- 遗留/约束：
  - better-sqlite3 为 native addon，必须用 Node 20（`.nvmrc`/`engines` 已约束）；在 Node 24+ 下运行测试会出现 NODE_MODULE_VERSION 不匹配。

## 2026-02-25 14:56 - 任务：完成 Task 14（Todo 本地读写服务：SQLite SoT + tombstone + outbox）

- 原始需求：实现 Todo 的本地读写服务，所有写入必须同一事务写业务表 + outbox_mutations；读列表必须分页且避免 N+1/查询风暴；deleted_at 软删/恢复语义要一致。
- 实现目标/逻辑：
  - 新增 TodoRepo（main 侧纯 DB 服务，便于单测与后续 IPC/UI 注入）：
    - listTodoLists/listTodoItems：单条 SQL + LIMIT/OFFSET；item 列表 JOIN todo_lists 以支持 includeArchivedLists 过滤；支持 list_id/status/tag/include_deleted 等过滤。
    - upsert/patch/delete/restore：对 todo_lists 与 todo_items 做本地写入；delete 采用 tombstone（deleted_at）而非物理删除；restore 清空 deleted_at。
  - outbox 集成：所有写入统一使用 withImmediateTransaction，并 enqueue outbox（resource=todo_list/todo_item；op=upsert/delete；data_json 对齐 apidocs 字段）。
  - 默认时区：未显式提供 tzid 时，使用系统 tz；若无法获取则回退到 Asia/Shanghai。
- 核心变更：
  - 新增：`src/main/todo/todoRepo.ts`
  - 新增单测：`src/main/todo/todoRepo.test.ts`
  - 证据：`.sisyphus/evidence/task-14-todo-outbox.txt`、`.sisyphus/evidence/task-14-todo-query.txt`
- 遗留/约束：
  - todo IPC 通道与 UI 集成将放到后续 UI/数据库生命周期任务中落地（保持 renderer 禁止 Node/SQLite）。

## 2026-02-25 15:16 - 任务：完成 Task 15（Collections 本地读写服务：树结构 + note_ref 混排 + 禁环）

- 原始需求：实现 collection_items 的本地读写服务（folder + note_ref 混排），写入必须同一事务写业务表 + outbox（resource=collection_item）；并强制防环（禁止把父节点移动到子孙节点下）。
- 实现目标/逻辑：
  - 新增 CollectionsRepo（main 侧纯 DB 服务，便于单测与后续 IPC/UI 注入）：
    - listCollectionItems：单条 SQL + LIMIT/OFFSET；当传 parentId 时仅返回直接子节点；不传 parentId 时返回全部（与服务端合同一致）。按 sort_order ASC, created_at ASC 稳定排序。
    - createFolder/createNoteRef：按 item*type 语义校验（folder 不允许 ref*_；note*ref 必须有 ref*_）；parent 必须是未删除的 folder。
    - patchCollectionItem：支持改名/改色/改排序/改 parent/改引用；改 parent 时做防环校验。
    - deleteCollectionItem：note_ref 仅 tombstone 自己；folder 递归 tombstone 子树（本地 UI 一致性），但仅为根 item 入队 outbox delete（服务端会级联 tombstone）。
    - restoreCollectionItem：清空 deleted_at，并入队 outbox upsert（revive 语义）。
  - outbox 集成：所有写入统一使用 withImmediateTransaction，并 enqueue outbox（resource=collection_item；op=upsert/delete；data_json 对齐 apidocs 形状）。
  - 防环实现：使用递归 CTE 沿 parent 链向上查祖先，命中自身 id 则拒绝。
- 核心变更：
  - 新增：`src/main/collections/collectionsRepo.ts`
  - 新增单测：`src/main/collections/collectionsRepo.test.ts`
  - 证据：`.sisyphus/evidence/task-15-outbox.txt`、`.sisyphus/evidence/task-15-no-cycle.txt`
- 遗留/约束：
  - Collections 的 IPC/UI、拖拽移动与批量 move（对齐服务端 move API 的 items[] 批处理）将在后续 UI/任务中落地。

## 2026-02-25 17:55 - 任务：完成 Task 16（Flow API Client：鉴权头 + 设备头 + OpenAPI 对齐）

- 原始需求：建立 Flow API 的类型与调用封装；每次请求必须注入 Authorization Bearer + X-Request-Id + 设备头；Flow Base URL 默认 https://xl.pscly.cc 并支持标准化。
- 实现目标/逻辑：
  - 新增 Flow client：
    - 复用 `createHttpClient` 作为底层（统一 request_id / ErrorResponse / 重试）。
    - 通过 defaultHeaders 注入：`Authorization: Bearer <token>`、`X-Flow-Device-Id`、`X-Flow-Device-Name`。
    - 提供后续 sync 引擎复用的调用：`syncPush`（POST /api/v1/sync/push）与 `syncPull`（GET /api/v1/sync/pull）。
  - 类型对齐：在 Flow client 内定义最小必要类型，并按 Collections 专题合同扩展：支持 `resource=collection_item` 与 `changes.collection_items`（可选字段，向后兼容）。
  - 单测覆盖：
    - 断言请求头包含 Authorization/X-Request-Id/X-Flow-Device-Id/X-Flow-Device-Name。
    - 断言 baseUrl 尾随 `/` 被标准化，最终请求 URL 不出现双斜杠。
- 核心变更：
  - 新增：`src/main/flow/flowClient.ts`
  - 新增单测：`src/main/flow/flowClient.test.ts`
  - 证据：`.sisyphus/evidence/task-16-flow-headers.txt`、`.sisyphus/evidence/task-16-baseurl.txt`
- 遗留/约束：
  - token/设备标识的持久化来源（TokenStore/DeviceIdentity store）将在登录/设置与 DB 生命周期任务中与 Flow client 完整接线。

## [2026-02-25 18:33] - 任务：Task 17 Flow Sync Push 引擎（applied/rejected 判定 + 429/401/413 策略）

- 原始需求：按 `PLAN.md:1.6.1.3` 完成 Flow `sync/push` 引擎收尾与验收落盘：严格逐条 applied/rejected（不能只看 HTTP 200）；429 遵守 Retry-After 并暂停本轮剩余 push（返回 rate*limited）；401 不自动重试（返回 need_relogin）；413/payload_too_large 支持自动拆分 batch（直到单条仍失败则 fatal）；outbox 状态机字段 attempt/next_retry_at_ms/last_error*\* 可追溯。
- 实现目标/逻辑：
  - 引擎每批 claim outbox（pending/failedRetryable 且 next_retry_at_ms<=now），标记 inflight 后调用 Flow `sync/push`。
  - 成功时以响应体 `applied[]/rejected[]` 逐条更新 outbox：applied->APPLIED；rejected(conflict)->REJECTED_CONFLICT 并保存 `rejected[].server` 快照；语义失败(reason=validation_error 等)->FAILED_FATAL；未知 reason 保守 FAILED_RETRYABLE。
  - 429：从 httpClient 暴露的 `retryAfterSeconds` 计算 next_retry_at_ms，并停止本轮（引擎返回 rate_limited）。
  - 401：停止本轮并返回 need_relogin；已尝试的条目保持 pending + 记录 last_error_code=unauthorized，等待用户重新登录后再推。
  - 413：遇到 HTTP 413 或 ErrorResponse `payload_too_large` 时对 mutations 自动二分拆分推送；若拆到单条仍失败则标记 FAILED_FATAL(payload_too_large)。
  - request_id 记录策略：outbox.request_id 优先取响应头 `X-Request-Id`，错误时回退到 ErrorResponse `request_id`。
- 核心变更：
  - 修改：`src/main/flow/flowSyncPush.ts`（补齐 outbox.request_id 的持久化优先级：header -> ErrorResponse.request_id）
  - 修改：`src/main/flow/flowSyncPush.test.ts`（补充断言：applied/rejected/429/401/413 场景下 request_id 落库）
  - 证据：`.sisyphus/evidence/task-17-tests.txt`、`.sisyphus/evidence/task-17-typecheck-build.txt`
- 遗留/约束：
  - 目前“自动拆分(413)后最终成功”的场景不会在 outbox 层保留触发拆分的那次 413 request_id；后续需要在诊断/日志体系（Task 27）中记录“发生过 413 拆分”事件以满足可观测性要求。

## [2026-02-25 19:28] - 任务：Task 18 Flow Sync Pull 引擎（cursor + has_more 循环 + apply 后推进）

- 原始需求：实现 Flow `sync/pull` 的增量拉取引擎：从 `sync_state` 读取 cursor（无则 0），按 `has_more` 循环拉取；每一轮必须严格使用上一轮的 `next_cursor`；changes apply 成功落库后才允许推进 cursor；任一轮 apply 失败必须停止且保持旧 cursor；支持 tombstone（`deleted_at != null`）；未知 changes key 容错忽略但记录日志；显式处理 `changes.collection_items`。
- 实现目标/逻辑：
  - 新增 `runFlowSyncPull`：循环调用 `FlowClient.syncPull({ cursor, limit })`。
  - 每一轮把“apply changes + 写回 sync_state cursor=next_cursor”放在同一 `withImmediateTransaction` 事务内，保证原子性（apply 成功才推进 cursor）。
  - pull apply 直写业务表（`todo_lists/todo_items/todo_occurrences/collection_items/user_settings`），不走 TodoRepo/CollectionsRepo 写路径，避免 enqueue outbox。
  - LWW：使用 `INSERT ... ON CONFLICT DO UPDATE ... WHERE excluded.client_updated_at_ms >= table.client_updated_at_ms`，incoming 更旧时不覆盖本地。
  - todo_items：`tags/reminders` 分别序列化落到 `tags_json/reminders_json`；`archived/is_recurring` 映射为 0/1。
  - 未知 changes key：仅记录 key（`console.warn`），避免把 changes 内容写入日志。
- 核心变更：
  - 新增：`src/main/flow/flowSyncPull.ts`
  - 新增：`src/main/flow/flowSyncPull.test.ts`
  - 新增证据：`.sisyphus/evidence/task-18-pull-loop.txt`、`.sisyphus/evidence/task-18-cursor-rollback.txt`
- 遗留/约束：
  - `changes.notes` 当前不落库（FlowNotes 降级表属于后续任务），本次仅不将其视为“未知 key”并跳过。

## [2026-02-25 19:41] - 任务：Task 23 memoName 编码规则落地（route encode/decode + key base64url）

- 原始需求：memoName 可能包含 `/`（例如 `memos/123`），任何放进路由/URL/文件名/KV key 的地方都必须可 round-trip 编码/解码。
- 实现目标/逻辑：
  - 路由/URL 参数：统一使用 `encodeURIComponent` / `decodeURIComponent`。
  - 本地 KV key：统一使用 base64url(utf8)（`-`/`_` 替代 `+`/`/`，去掉 `=` padding；解码时按长度补齐 padding）。
  - 实现必须同时可在 Node(main) 与浏览器(renderer) 环境运行（Node 用 Buffer；浏览器用 atob/btoa）。
- 核心变更：
  - 新增：`src/shared/memoNameCodec.ts`
  - 新增：`src/shared/memoNameCodec.test.ts`
  - 证据：`.sisyphus/evidence/task-23-memoname.txt`、`.sisyphus/evidence/task-23-no-path.txt`

## 2026-02-25 20:02 - 任务：Task 34 Memos API Client（Notes 直连：分页/过滤/updateMask/附件端点）

- 原始需求：实现 main 侧 Memos API client（Notes 直连），覆盖 memos CRUD、分页/过滤、updateMask 强制校验与附件相关端点，并提供 Vitest 单测与证据落盘。
- 实现目标/逻辑：
  - 复用 `createHttpClient`，每次请求通过 defaultHeaders 注入 `Authorization: Bearer <token>`；query 参数通过 `requestJson({ query })` 传入。
  - API 路径统一走 `/api/v1/*`；对资源名（如 `memos/123`、`attachments/1`）仅作为“API resource name”处理，拼接为请求 pathname（不当成本地路径片段）。
  - 更新接口强制要求 `updateMask`：缺失/空值直接抛出中文可解释错误，避免误更新。
  - 附件端点覆盖：Create/Get/Update/Delete Attachment，以及 List/Set Memo Attachments。
- 核心变更：
  - 新增：`src/main/memos/memosClient.ts`
  - 新增：`src/main/memos/memosClient.test.ts`
  - 新增证据：`.sisyphus/evidence/task-34-updatemask.txt`、`.sisyphus/evidence/task-34-pagination.txt`
- 遗留/约束：
  - 本仓库的 SQLite 相关测试依赖 `better-sqlite3` 原生模块，运行测试建议固定使用 Node 20（本次用 `nvm use 20.20.0` 跑通）。

## [2026-02-25 20:22] - 任务：Task 35 Notes Router（provider 决策树 + 单次请求降级一次 + request_id 分离展示）

- 原始需求：落地 `PLAN.md:1.4.1.2` 的 Notes 路由决策树；仅在 Memos 401/403 或 NETWORK/TIMEOUT 时允许当次请求降级到 FlowNotes 重试一次；有效 HTTP 非 401/403 不得降级；降级时分离展示 `memos_request_id` 与 `flow_request_id`；结果必须可区分来源 `[Memos]` / `[FlowNotes]`。
- 实现目标/逻辑：
  - 新增纯函数路由器 `routeNotesRequest`，通过依赖注入 `memosRequest/flowNotesRequest` 在 Node/Vitest 环境可测试。
  - 路由裁决顺序：
    - `memosBaseUrl` 缺失/空/非法/非 http(s)（通过 `normalizeBaseUrl` 校验）→ 直接选择 FlowNotes（不尝试 Memos）。
    - 默认先请求 Memos；若 Memos 返回 401/403 或错误码为 `NETWORK_ERROR/TIMEOUT` → 当次请求降级到 FlowNotes 重试一次。
    - 若 Memos 返回有效 HTTP 且非 401/403（4xx/5xx/429 等）→ 不降级，直接暴露 `[Memos]` 错误。
  - request_id 规则（Notes 专用）：优先使用响应头 `X-Request-Id`（httpClient 的 `responseRequestIdHeader`），否则回退到客户端 `requestId`；降级重试时分别输出 `memos_request_id` 与 `flow_request_id`。
- 核心变更：
  - 新增：`src/main/notes/notesRouter.ts`
  - 新增：`src/main/notes/notesRouter.test.ts`（覆盖 5 条规则 + “仅降级一次”）
  - 新增证据：`.sisyphus/evidence/task-35-degrade-auth.txt`、`.sisyphus/evidence/task-35-no-degrade.txt`
- 遗留/约束：
  - 本任务仅实现 Router 核心逻辑与单测，不实现 FlowNotes API Client（Task 52）。
  - 本仓库测试需在 Node 20 下运行（better-sqlite3 native addon 约束），执行命令需先 `source ~/.nvm/nvm.sh && nvm use 20.20.0`。

## [2026-02-25 20:24] - 任务：Task 35 补充修正（typecheck 收敛）

- 原始需求：在不改变决策树语义的前提下，确保 `npm run typecheck` 通过。
- 核心变更：
  - `src/main/notes/notesRouter.ts`：将 degraded 分支的 `providerLabel` 固定为字面量 `'[FlowNotes]'`，避免 TypeScript 因联合类型无法收窄导致的类型错误。

## [2026-02-25 20:38] - 任务：Task 36 Notes（Memos）本地表 Schema + 同步状态机字段

- 原始需求：新增 migration v4，创建 `memos` 与 `memo_attachments` 两张表，并为 `sync_status` 增加受控值域（LOCAL_ONLY/DIRTY/SYNCING/SYNCED/FAILED）；提供 schema 单测与证据落盘；同时证明不会把 `memos/123` 当成本地文件路径片段。
- 实现目标/逻辑：
  - 在 `src/main/db/migrations.ts` 末尾追加 version=4（版本号必须从 1 连续递增）。
  - `memos.sync_status` 使用 SQLite `CHECK(sync_status IN (...))` 强约束，避免状态机字段“自由文本”导致后续同步逻辑不可控。
  - `memo_attachments` 以 `memo_local_uuid -> memos.local_uuid` 外键关联，并启用 `ON DELETE CASCADE`，便于 memo 删除时清理附件记录。
  - 通过 `rg` 输出证明 `server_memo_name/memoName` 未被拼接进 `path.join/resolve`。
- 核心变更：
  - 修改：`src/main/db/migrations.ts`（新增 v4：memos + memo_attachments + 索引 + sync_status CHECK）
  - 新增：`src/main/db/memosSchema.test.ts`（表存在 + 非法 sync_status 插入失败）
  - 新增证据：`.sisyphus/evidence/task-36-memos-schema.txt`、`.sisyphus/evidence/task-36-no-path.txt`
- 遗留/约束：
  - 运行 DB 测试需 Node 20（better-sqlite3 native addon 约束）。
  - `server_memo_name` 仅作为服务端资源名持久化，不得用于本地路径/文件名拼接；本任务已落盘 rg 证据。

## [2026-02-25 20:43] - 修正：Task 36 `memos.server_memo_id` 类型偏差

- 原始问题：`memos.server_memo_id` 误建为 INTEGER，但 Memos API 的 memo id 语义为 string（可能是 uuid/自定义 id），需要能承载非纯数字。
- 修正内容：在 migration v4 的 `memos` 表定义中将 `server_memo_id` 从 `INTEGER NULL` 改为 `TEXT NULL`；索引/约束语义保持不变（partial unique 仍按 NOT NULL 生效）。
- 验证：`npm test` 与 `npm run typecheck` 已通过。

## [2026-02-27 16:00] - 任务：Notes 草稿 autosave IPC 打通（renderer 可调用）

- 原始需求：把 Notes 草稿 autosave 能力通过 IPC 打通到 renderer 可调用（不做 renderer UI、不做 Playwright E2E）。
- 实现目标/逻辑：
  - 在 `IPC_CHANNELS.notes` 下新增 `createDraft/upsertDraft/getDraft` 三个通道，并补齐 payload/result 类型。
  - main 侧在 `registerIpcHandlers()` 逐条注册 `ipcMain.handle(...)`，并新增 payload 校验函数；校验失败统一返回 `VALIDATION_ERROR`。
  - main 注入 `deps.notes`：草稿相关方法使用 `withMainDbRef + createNotesDraftRepo` 复用本地写库逻辑（由 repo 保证写入 memos 且 `sync_status='DIRTY'`）。
  - preload 与 `vite-env.d.ts` 同步补齐 `window.xinliu.notes` API，保证 renderer 侧类型与运行时一致。
- 核心变更：
  - shared：`src/shared/ipc.ts`（新增 notes 草稿通道与类型）
  - main：`src/main/ipc.ts`、`src/main/main.ts`（注册 handler + deps 注入）
  - preload/renderer：`src/preload/index.ts`、`src/renderer/vite-env.d.ts`（暴露与类型声明）
- 遗留/约束：
  - notes 的 `listItems/delete/restore/hardDelete` 暂未实现，当前保持可解释错误（不静默）。

## [2026-02-25 21:10] - 任务：完成 Task 42（Flow Auth：登录/注册/登出 + 失效处理状态）

- 原始需求：实现 Flow Auth 的登录/注册（`POST /api/v1/auth/login`、`POST /api/v1/auth/register`），token 必须写入 TokenStore（keytar 优先，fallback 内存）；登出清除 TokenStore；遇到 401 unauthorized 时进入“需要重新登录”的可见状态；登录成功后至少暴露“token 已保存”与 `server_url`。
- 实现目标/逻辑：
  - FlowAuthClient：复用 `createHttpClient`，但不注入 Authorization（登录/注册不依赖已登录 token），直接调用 `/api/v1/auth/login|register`。
  - AuthService：通过依赖注入组合 `tokenStore + flowAuthClient`，维护可观测状态 `getState()`：`signed_out/signed_in/reauth_required` + `reauthRequired` + `serverUrl`；
    - login/register 成功：写入 tokenStore，并返回 `tokenSaved=true` 与 `serverUrl`。
    - logout：清除 tokenStore，并清空内存态（serverUrl 置空）。
    - 401 判定：沿用同步引擎模式 `status===401 || errorResponse.error==='unauthorized'`，触发 `reauth_required` 并清理 tokenStore。
- 核心变更：
  - 新增：`src/main/flow/flowAuthClient.ts`、`src/main/auth/authService.ts`
  - 新增单测：`src/main/auth/authService.test.ts`
  - 证据：`.sisyphus/evidence/task-42-auth.txt`
  - 遗留/约束：
  - 本任务仅在用例返回值中暴露 `serverUrl`（作为后续 memosBaseUrl 来源），不涉及设置持久化与 UI/IPC。

## [2026-02-25 21:30] - 任务：Task 51 Flow Notes（降级 provider）本地 notes 表 Schema + 使用边界

- 原始需求：实现“Flow Notes（降级 provider）本地表 Schema + 使用边界（仅降级时可读写）”。
- 实现目标/逻辑：
  - SQLite 迁移新增 `notes` 表，仅用于 FlowNotes 降级路径的本地镜像/诊断；支持 tombstone（`deleted_at`）。
  - 在代码层强制边界：只有当 Notes Router 的 per-request 路由结果为 `kind='degraded'`（即 Flow Notes(degraded)）时，才允许对该表读写；否则抛出中文可解释错误，避免隐式双写。
  - 表结构至少包含诊断/回放字段：`last_request_id`（request_id）、`last_error`、`provider_reason`。
- 核心变更：
  - `src/main/db/migrations.ts`：新增 migration v5 `notes_flow_notes_degraded_local_cache`（创建 `notes` 表 + 索引）。
  - `src/main/notes/flowNotesDegradedNotesRepo.ts`：新增 repo，并在每次读写时检查路由结果必须为 degraded。
  - `src/main/db/flowNotesDegradedNotes.test.ts`：新增 Node 环境单测（表存在 + 边界规则）。
  - 证据：`.sisyphus/evidence/task-51-boundary.txt`。
- 遗留/约束：
  - 该表仅作为降级承载/离线镜像，不引入任何 Flow Notes 网络 client（留待后续任务）。

## [2026-02-25 21:45] - 修正：Task 51 notes 表访问边界（single flow_notes 也允许）

- 修正内容：本地 `notes` 表的访问护栏应以 Notes Router 的“本次请求最终 provider”为准：只要最终 `provider==='flow_notes'` 即允许访问。因此除了 `kind='degraded'`（fallback，使用 `degradeReason`）之外，`kind='single'` 且 `provider='flow_notes'`（例如 memosBaseUrl 非法导致直接选 FlowNotes，使用 `providerReason`）也应被允许。

## [2026-02-25 22:10] - 任务：Task 52 Flow Notes API Client（/api/v1/notes\*：CRUD + attachments + shares + revisions）

- 原始需求：实现 main 侧 Flow Notes API client，覆盖 Notes CRUD、附件上传（multipart/form-data，字段名 `file`）、Shares 创建、Revisions 列表与还原；写操作必须仅在 Notes Router 的 per-request 最终 provider 为 `flow_notes` 时允许；并新增单测覆盖 multipart 与 409 conflict 解析（暴露 `details.server_snapshot`）。
- 实现目标/逻辑：
  - 扩展 `createHttpClient`：在保留 `requestJson` 的同时新增通用 `request`，允许透传非 JSON 请求体（用于 multipart 上传），并继续沿用 request_id 注入与 ErrorResponse 解析。
  - 新增 `createFlowNotesClient`：对齐 OpenAPI `/api/v1/notes*` 端点实现 CRUD、revisions、shares、attachments；对所有错误统一在 message 前加 `[FlowNotes]`，确保来源可区分。
  - 写入边界：抽取并复用 `requireFlowNotesFinalDecision(decision)`（允许 degraded 与 single flow_notes），FlowNotes client 的所有写方法必须显式接收 decision 并在运行时校验。
- 核心变更：
  - `src/main/net/httpClient.ts`：新增 `request` 方法，支持 multipart。
  - 新增：`src/main/notes/flowNotesClient.ts`、`src/main/notes/flowNotesDecisionGuard.ts`
  - `src/main/notes/flowNotesDegradedNotesRepo.ts`：复用新的 decision guard。
  - 新增单测：`src/main/notes/flowNotesClient.test.ts`
  - 新增证据：`.sisyphus/evidence/task-52-multipart.txt`
- 遗留/约束：
  - 本任务仅落地 main-side client 与单测，不涉及 renderer/UI/IPC。

## [2026-02-25 23:10] - 任务：Task 38 Memos Sync Job（create/update + 附件上传顺序 + 回拉合并保护本地）

- 原始需求：实现 main 侧同步单条 memo 的 job：
  - 无 `server_memo_id` 时走 CreateMemo；否则走 UpdateMemo（必须提供 updateMask）。
  - 附件必须先上传（CreateAttachment 拿到服务端 name），再绑定到 memo（SetMemoAttachments）。
  - 回拉/刷新合并时，禁止覆盖本地 `DIRTY`/`SYNCING` 的 content（仅更新安全元数据）。
- 实现目标/逻辑：
  - 新增 `runMemosSyncOneMemoJob`：从本地 SQLite 读取 memo 与 memo_attachments，标记 SYNCING → 执行 create/update → 逐个上传缺失 server_attachment_name 的附件 → SetMemoAttachments → 成功置 SYNCED；失败置 FAILED 并记录 last_error。
  - 新增 `runMemosRefreshOneMemo` + `mergePulledServerMemoIntoLocalMemo`：用于“回拉/刷新”时合并 server memo；当本地 sync_status 为 DIRTY 或 SYNCING 时，禁止覆盖 content/visibility，仅回填 server id/name 等安全字段。
  - 单测离线确定性：对 memosClient 完全 mock；附件内容读取通过可注入 `loadAttachmentContentBase64` 代替真实磁盘 IO。
- 核心变更：
  - 新增：`src/main/memos/memosSyncJob.ts`（同步/刷新/合并逻辑）
  - 新增：`src/main/memos/memosSyncJob.test.ts`（Vitest Node：create vs update、附件顺序、DIRTY/SYNCING 不覆盖）
  - 新增证据：`.sisyphus/evidence/task-38-attach-order.txt`、`.sisyphus/evidence/task-38-no-overwrite.txt`
- 遗留/约束：
  - 本任务仅实现“单条 memo 同步 job + 回拉合并保护”，不涉及 job 表调度器、附件缓存/LRU、renderer/preload/IPC 接线（按计划后续任务处理）。

## [2026-02-28 18:28] - 任务：Task 39 Notes 冲突副本策略（保留本地文本 + 原记录回滚为服务端 + 可对比可复制）

- 原始需求：当直连 Memos 的更新遇到 409 conflict 时，必须保留本地正文为“冲突副本”，并把原记录回滚为服务端版本；两份都能被检索/读取；renderer 提供最小对比入口契约（`data-testid="conflict-compare"`）。
- 实现目标/逻辑：
  - 在 `runMemosSyncOneMemoJob` 的 UpdateMemo 路径识别 HTTP 409：优先读取 `ErrorResponse.details.server_snapshot`（如缺少 `content/visibility` 则补 `GetMemo`），随后执行“冲突副本 + 回滚”同一事务写入。
  - 冲突副本落在 `memos` 表（`sync_status='LOCAL_ONLY'`），并新增诊断字段：`conflict_of_local_uuid`（指向原记录）与 `conflict_request_id`（优先响应头 `X-Request-Id`）。
  - 原记录回滚为服务端正文/可见性并标记 `sync_status='SYNCED'`；原记录下附件行移动到冲突副本名下，避免正文回滚后附件错配。
  - 通过 `queryGlobalSearch` 在单测里断言“本地副本与回滚后的原记录都可检索”，证明两份可读取/检索。
- 核心变更：
  - 新增：`src/main/notes/memoConflictCopy.ts`（冲突副本生成与回滚的 DB 事务逻辑）
  - 修改：`src/main/db/migrations.ts`（v8：memos 增加 conflict 元数据列与索引）
  - 修改：`src/main/memos/memosSyncJob.ts`（UpdateMemo 409 分支返回 kind='conflict' 并触发冲突副本/回滚）
  - 修改：`src/main/memos/memosSyncJob.test.ts`（新增 409 冲突单测，修正 search item 字段为 id）
  - 修改：`src/renderer/App.tsx`（冲突页加入最小对比入口 `data-testid="conflict-compare"`）
- 遗留/约束：
  - 当前只覆盖 UpdateMemo 的 409 冲突；CreateMemo 409 与其它并发/拒绝冲突形态留待后续任务（Task 41 冲突中心）在有证据的前提下扩展。

## [2026-02-26 04:23] - 任务：Task 19 托盘常驻 + 关闭语义 + 唯一退出路径（Tray Manager）

- 原始需求：主进程启用托盘常驻；关闭按钮不退出而是隐藏到托盘；托盘菜单提供显示/隐藏、快速捕获入口、立即同步（Memos/Flow）、打开设置、退出；退出是唯一真正 quit 路径并执行清理 hooks；补齐单测与证据。
- 实现目标/逻辑：
  - main 进程强引用 Tray（避免 GC）并使用 `.ico` 图标；安装托盘菜单与左键点击切换主窗口显示。
  - 关闭语义：拦截 BrowserWindow `close` 事件，默认 `preventDefault + hide()`；仅当用户通过托盘“退出”或 app `before-quit` 标记退出意图时才允许真实关闭。
  - 首次关闭提示：在“关闭到托盘”路径上仅提示一次（进程生命周期内一次），退出路径不提示。
  - 唯一退出：托盘“退出”触发 `closeToTray.requestExit()` → 依次执行注入的 cleanup hooks（当前以数组占位，后续任务接线）→ 销毁 tray → `app.quit()`。
- 核心变更：
  - 新增：`src/main/tray/trayManager.ts`、`src/main/tray/trayManager.test.ts`
  - 修改：`src/main/main.ts`（安装 close-to-tray、托盘菜单、首次关闭提示 Notification、window-all-closed no-op）
  - 新增：`assets/tray.ico`
  - 证据：`.sisyphus/evidence/task-19-close-to-tray.txt`、`.sisyphus/evidence/task-19-exit-cleanup.txt`
- 遗留/约束：

## [2026-02-26 05:40] - 任务：Task 20 全局快捷键管理 + 设置页配置（注册失败可见退路）

## [2026-02-26 10:00] - 任务：Task 28 全局搜索（FTS5 + 单次 IPC 分页返回 + 快捷键入口）

- 原始需求：落地全局搜索能力：SQLite FTS5 索引覆盖 Notes/Todo/Collections；IPC 单次返回一页（禁止 N+1）；renderer 主窗搜索框接入并支持快捷键聚焦；FTS 不可用时提示可重建并走降级查询；补齐单测与 evidence。
- 实现目标/逻辑：
  - SQLite：新增 migration v7（global_search_fts），并用触发器维护索引；为避免运行环境缺少 FTS5 导致迁移整体失败，v7 在创建虚表失败时直接 return（迁移仍推进到 v7，后续查询会检测表缺失并降级）。
  - 查询：main 侧实现 `queryGlobalSearch`（优先 FTS5，失败/缺失时 fallback），单次查询直接返回一页 items + hasMore + mode/ftsAvailable。
  - IPC：新增 `IPC_CHANNELS.search.query/rebuildIndex`，main 显式注册 handler，preload 仅暴露 `window.xinliu.search.*` 用例级 API。
  - Renderer：`GlobalSearchBox` 真实接入 IPC 查询；当 `ftsAvailable=false` 显示“已降级 + 可重建索引”，并提供重建按钮；分页通过“加载更多”进行下一页请求。
- 核心变更：
  - SQLite：`src/main/db/migrations.ts`（v7，自容错）
  - main：`src/main/search/globalSearch.ts`、`src/main/main.ts`（search IPC deps + 正常 applyMigrations）
  - IPC：`src/shared/ipc.ts`、`src/main/ipc.ts`、`src/preload/index.ts`、`src/renderer/vite-env.d.ts`
  - renderer：`src/renderer/App.tsx`、`src/renderer/styles.css`、`src/renderer/App.test.tsx`（补齐 mock search）
  - tests：`src/main/search/globalSearch.test.ts`
  - evidence：`.sisyphus/evidence/task-28-fts.txt`、`.sisyphus/evidence/task-28-no-storm.txt`
- 遗留/约束：
  - `// @vitest-environment node` 是 DB 测试所需指令（better-sqlite3 需 Node 环境）。

## [2026-02-26 07:45] - 任务：Task 24 实现 memo-res:// 协议处理器（白名单目录 + 防穿越 + MIME 白名单）

- 原始需求：实现 `memo-res://<cacheKey>` 自定义协议作为 renderer 预览附件的唯一入口；协议层只做读取路由；强制安全边界（白名单目录、拒绝穿越/编码绕过、拒绝 symlink/junction、MIME 白名单与高风险强制下载），且不得泄漏绝对路径。
- 实现目标/逻辑：
  - 在 `src/main/protocol/memoResProtocol.ts` 提供“纯逻辑 handler + 协议安装薄适配器”的结构：核心 handler 仅依赖注入 `resolveCacheKey/readFile/lstat`，便于 Vitest(Node) 覆盖安全边界，不导入 Electron runtime。
  - cacheKey 解析同时兼容 `memo-res://<cacheKey>`（hostname）与 `memo-res:///cacheKey`（pathname）两种形态；cacheKey 必须为不透明 key（仅 `[A-Za-z0-9_-]`），拒绝 `/`、`\`、`%` 等。
  - 访问控制：resolver 返回 relpath 后先做白名单目录（仅 `attachments-cache/` 与 `attachments/`）裁决，再用 `fromRelpath(<root>, relpath)` 防越界；随后逐段 `lstat` 拒绝路径链路中的 symlink/junction。
  - MIME/Disposition：仅对白名单图片扩展名返回 `image/* + inline`；未知或高风险扩展名一律 `application/octet-stream + attachment`，并加 `X-Content-Type-Options: nosniff`。
- 核心变更：
  - 新增：`src/main/protocol/memoResProtocol.ts`（核心 handler + installMemoResProtocol）
  - 新增：`src/main/protocol/memoResProtocol.test.ts`（Vitest Node：host/pathname、非法 key、非白名单、symlink/junction、MIME/Disposition）
  - 修改：`src/main/main.ts`（`app.whenReady()` 后注册 `memo-res` 协议；cacheKey->relpath 最小实现为查询 SQLite `memo_attachments`：`cache_relpath ?? local_relpath`；DB 不存在/表不存在返回 null，不 throw）
  - 新增证据：`.sisyphus/evidence/task-24-protocol-security.txt`、`.sisyphus/evidence/task-24-no-download.txt`
- 遗留/约束：
  - 本仓库含 `better-sqlite3` 原生模块，验证需在 Node 20（`nvm use 20.20.0`）下运行。
- 原始需求：
  - main 进程在 `app.whenReady()` 后注册全局快捷键（至少 2 个：打开快捕入口、打开主窗并聚焦搜索框），renderer 禁止直接注册。
  - 设置页可修改/禁用/恢复默认；当 `globalShortcut.register()` 返回 `false` 时必须在设置页可见并引导改键。
  - 退出（will-quit）必须 `unregisterAll()`。
- 实现目标/逻辑：
  - 按现有 IPC 白名单模式在 `src/shared/ipc.ts` 追加 `shortcuts:*` 通道与 `IPC_EVENTS.shortcuts.focusSearch` 事件名；main 侧显式注册 handlers 并统一返回 `IpcResult`。
  - main 侧实现 `ShortcutsManager`（纯逻辑 + DI），用 `activeAcceleratorById` 跟踪每条快捷键上一次注册的 accelerator，确保变更/禁用/置空时先注销旧值，避免残留注册。
  - preload 暴露 `window.xinliu.shortcuts.*` 用例级 API，并提供 `onFocusSearch` 事件订阅；renderer 仅通过该 API 读写配置与查询注册状态。
  - renderer 设置页渲染“快捷键配置”区块（`data-testid="settings-shortcuts"`），每条快捷键显示注册状态与失败提示（`settings-shortcut-<id>-register-failed`）。
  - 搜索框聚焦：main 侧快捷键回调通过 `webContents.send(IPC_EVENTS.shortcuts.focusSearch)` 通知 renderer；renderer 右栏提供 `global-search-input` 占位输入框并响应 focusSearch。
- 核心变更：
  - 修改：`src/main/main.ts`（app ready 后 registerAll；will-quit 清理；回调触发 focusSearch 事件）
  - 新增：`src/main/shortcuts/shortcutManager.ts`、`src/main/shortcuts/shortcutManager.test.ts`
  - 修改：`src/shared/ipc.ts`、`src/main/ipc.ts`、`src/main/ipc.test.ts`（新增 shortcuts IPC 白名单与校验）
  - 修改：`src/preload/index.ts`、`src/renderer/vite-env.d.ts`（暴露与类型补齐）
  - 修改：`src/renderer/App.tsx`、`src/renderer/styles.css`、`src/renderer/App.test.tsx`（设置页 UI + 注册失败可见测试 + 搜索框聚焦占位）
  - 证据：`.sisyphus/evidence/task-20-shortcut-fail.txt`、`.sisyphus/evidence/task-20-shortcut-cleanup.txt`
- 遗留/约束：
  - 快捕窗真实窗口（Task 21）尚未实现：当前“打开快捕窗”快捷键暂时复用托盘“快速捕获”回调（show 主窗）。
  - cleanup hooks 以 DI 注入为目标，本任务仅提供可注入执行点，不实现同步调度器/快捷键/快速捕获窗口。

## [2026-02-26 06:18] - 任务：Task 21 快速捕捉窗口（Quick Capture）

- 原始需求：提供“快速捕获”独立小窗，支持全局快捷键、托盘菜单、应用内按钮三入口；窗口内快速输入并通过 Enter 保存隐藏，Esc 取消隐藏；后续可对接 Notes/SQLite。
- 实现目标/逻辑：
  - 入口三连：全局快捷键、托盘“快速捕获”、以及主窗口自定义标题栏的“快捕”按钮（作为无快捷键/托盘时的兜底入口）。
  - 快捕窗路由策略：main 侧为快捕窗 `loadFile(..., { hash: 'quick-capture' })`，renderer 侧用 `window.location.hash === '#quick-capture'` 在同一 bundle 内切分视图并渲染 `QuickCaptureWindow`。
  - 键盘交互：Enter 触发 submit，调用保存流程后直接 hide；Esc 触发 cancel，直接 hide。
  - 保存落点：仅提供可注入 `saveQuickCapture(content)` 作为未来持久化入口，当前生产实现为 no-op（后续接 Notes/SQLite）。
- 核心变更：
  - main：`src/main/main.ts` 已接线 quick capture controller + window manager，并把 `openQuickCapture` 快捷键与 tray “快速捕获”指向快捕窗。
  - IPC：`src/shared/ipc.ts` 新增 quickCapture channels；`src/main/ipc.ts` 注册 handlers；`src/preload/index.ts` 暴露 `window.xinliu.quickCapture.*`。
  - renderer：`src/renderer/App.tsx` 通过 `#quick-capture` 渲染 `QuickCaptureWindow`；titlebar 新增 `data-testid="titlebar-quick-capture"` 作为应用内兜底入口。
  - 单测/证据：`src/main/quickCapture/quickCaptureController.test.ts` 包含用例“Enter 保存隐藏”；证据：`.sisyphus/evidence/task-21-enter-hide.txt`。
- 遗留/约束：
  - “保存”暂不落库，仅保留 `saveQuickCapture(content)` 注入点；后续需要与 Notes/SQLite 以及同步策略一起收口实现。
  - Windows-only；快捕窗与主窗均保持 frameless 与 renderer 禁 Node 的安全约束不变。

## [2026-02-26 06:52] - 任务：Task 22（仅 UI）：设置页增加“数据存储目录”展示 + 迁移后重启提示

- 原始需求：设置页展示当前数据存储目录，并提供“更改目录”按钮；迁移成功后提示需要重启并提供“立即重启”按钮；补齐 renderer 单测。
- 实现目标/逻辑：
  - renderer 设置页新增 `data-testid="settings-storage-root"` 区块，展示 `storageRootAbsPath/isDefault`。
  - 进入设置页时，在 `openSettingsRoute()` 内同时刷新快捷键状态与 storageRoot 状态（`Promise.all([refreshShortcuts(), refreshStorageRoot()])`）。
  - 点击“更改目录”调用 `window.xinliu.storageRoot.chooseAndMigrate()`；当返回 `kind:'migrated'` 时展示 `data-testid="settings-restart-required"` 提示与“立即重启”按钮（调用 `storageRoot.restartNow()`）。
  - 空 API 降级：当 `window.xinliu?.storageRoot` 不存在时，在区块内显示可解释提示（calloutWarn）并禁用按钮，避免 UI 崩溃。
  - 单测离线确定性：测试内直接 stub `window.xinliu.storageRoot.*`，模拟 `chooseAndMigrate` 返回 `kind:'migrated'`，断言重启提示与按钮出现，并点击“立即重启”验证调用。
- 核心变更：
  - 修改：`src/renderer/App.tsx`（新增 Storage Root 设置区块 + 迁移后重启提示 + 进入设置页时刷新 storageRoot 状态）
  - 修改：`src/renderer/App.test.tsx`（新增迁移成功后重启提示用例）
- 遗留/约束：
  - 本任务仅补齐 UI 与测试，不修改 main/preload/shared；实际目录选择/迁移逻辑由既有 IPC 实现负责。

## [2026-02-26 07:08] - 修正记录：Task 22 实际为“Storage Root 更改与自动迁移（含回滚 + 重启提示）”，不止 UI

- 原始需求：完成并收尾计划项：`22. Storage Root 更改与自动迁移（含回滚 + 重启提示）`。
- 修正说明：上一条记录将 Task 22 误写为“仅 UI”，且遗漏了 main/preload/shared 的真实实现。Task 22 的完整实现包含：StorageRoot 配置文件（storage-root.json）、迁移引擎（staging -> rename 提交、失败回滚）、IPC 桥接、preload 暴露、renderer 设置页 UI 与重启提示。
- 实现目标/逻辑（完整链路）：
  - StorageRoot 配置文件：
    - 配置落点为 `storage-root.json`（位于 Electron `app.getPath('userData')` 目录下）。
    - 通过 “写入 tmp 文件 -> rename 覆盖” 的方式保证写入尽量原子；必要时删除旧文件后重试；最终兜底为直接写入（并清理 tmp）。
  - 迁移引擎（自动迁移 + 回滚）：
    - 迁移采用 staging 目录（`.xinliu-migrate-staging-<nowMs>-<randomId>`）先复制再提交：先把旧目录中的 `db/xinliu.sqlite3` 及其 `-wal/-shm`、`attachments-cache/`、`logs/` 复制到 staging，然后通过 `rename(stagingSubDir -> newRootSubDir)` 作为“提交”。
    - 失败回滚：任何步骤抛错时，清理已提交到新目录的目标（按反向顺序删除），并清理 staging 目录，保证“新目录不留半成品，旧目录保持可用”。
    - 保护：若目标目录已存在关键目录（db/attachments-cache/logs）则拒绝迁移，避免覆盖/混写。
    - 迁移覆盖范围（当前实现）：db（含 WAL/SHM）、attachments-cache、logs；旧目录默认保留不动。
  - IPC 与重启提示：
    - renderer 通过 `storageRoot.chooseAndMigrate()` 触发主进程打开目录选择对话框并执行迁移；迁移成功后写入 `storage-root.json` 并返回 `restartRequired: true`。
    - “立即重启”调用 `storageRoot.restartNow()`；主进程执行 `app.relaunch()` + `app.exit(0)`。
  - UI 契约：设置页存在 `data-testid="settings-storage-root"`；迁移成功后显示 `data-testid="settings-restart-required"`。
- 核心变更（真实涉及文件）：
  - main：
    - `src/main/storageRoot/storageRootConfig.ts`（storage-root.json 读写与原子落盘策略）
    - `src/main/storageRoot/migrateStorageRoot.ts`（staging 复制 + rename 提交 + 失败回滚清理）
    - `src/main/storageRoot/migrateStorageRoot.test.ts`（成功迁移/失败可回滚/目标目录冲突保护）
    - `src/main/main.ts`（接线：chooseAndMigrate 打开 dialog、调用迁移、写配置、restartNow relaunch/exit）
    - `src/main/ipc.ts`（storageRoot IPC handler 白名单/返回形状）
  - preload/shared：
    - `src/preload/index.ts`（暴露 `window.xinliu.storageRoot.*` 用例级 API）
    - `src/shared/ipc.ts`（StorageRoot IPC types：Status/ChooseAndMigrateResult 等）
  - renderer：
    - `src/renderer/App.tsx`（设置页 Storage Root 区块 + 重启提示 UI）
    - `src/renderer/App.test.tsx`（设置页迁移后重启提示契约测试）
    - `src/renderer/vite-env.d.ts`（补齐 `window.xinliu.storageRoot` 类型）
- 遗留/约束：
  - 本仓库含 `better-sqlite3` 原生模块，测试/验证需在 Node 20（例如 `nvm use 20.20.0`）下运行。
  - 迁移引擎当前不搬运 attachments/tmp/exports 等目录；后续若合同要求扩展，需要同步扩展迁移覆盖范围与回滚清单。

## [2026-02-26 08:12] - 任务：Task 25 附件缓存合同落地（LRU/配额 + cacheKey 映射）

- 原始需求：在 main 侧落地附件三类引用（local_relpath/cache_relpath/远端引用仅再下载），实现缓存配额 `attachmentCacheMaxMb` 的 LRU 驱逐；维护 `cacheKey -> (local_relpath|cache_relpath)` 映射；renderer 预览仅使用 `memo-res://<cacheKey>`；并在 memo-res 成功读取后由 main 更新 LRU 的 `last_access_at_ms`。
- 实现目标/逻辑：
  - DB 层为 `memo_attachments` 增加 LRU 元数据列 `cache_size_bytes/last_access_at_ms`，并用 SQLite `randomblob` 回填缺失 `cache_key`；建立 partial unique index，保证映射唯一。
  - main 侧实现可测试的缓存驱逐函数：按 `last_access_at_ms`（缺失则回退 `created_at_ms`）从旧到新驱逐，驱逐仅清理 `cache_relpath/cache_size_bytes`，不影响 `local_relpath`。
  - memo-res 协议层保持“只读路由”，但在成功读取后通过依赖注入 best-effort 回调触发 main 侧 touch（不接受 renderer 上报，避免伪造）。
- 核心变更：
  - 新增：`src/main/attachments/attachmentCache.ts`、`src/main/attachments/attachmentCache.test.ts`
  - 修改：`src/main/db/migrations.ts`（新增 v6：memo_attachments LRU 元数据 + cache_key 回填 + unique index）
  - 修改：`src/main/protocol/memoResProtocol.ts`、`src/main/protocol/memoResProtocol.test.ts`（成功读取后触发 reportCacheKeyAccessed）
  - 修改：`src/main/main.ts`（实现 reportCacheKeyAccessed：写 DB 更新 last_access_at_ms）
  - 修改：`src/main/memos/memosSyncJob.test.ts`（插入 memo_attachments 时写入 cache_key，符合合同）
  - 证据：`.sisyphus/evidence/task-25-lru.txt`、`.sisyphus/evidence/task-25-cachekey.txt`
- 遗留/约束：
  - `attachmentCacheMaxMb` 作为设置项的 UI/IPC 接线将在后续“设置页/本地设置持久化”任务统一落地；本任务先提供 `maxBytes` 参数化的配额驱逐核心逻辑与单测。

## [2026-02-26 08:36] - 任务：Task 26 右键菜单（中栏条目 + 左栏 Folder 树）

- 原始需求：实现右键菜单（Context Menu）。中栏条目右键：打开/移动到/删除/导出；左栏 Folder 右键：新建子项/重命名/移动/删除。约束：菜单触发动作必须走 IPC；renderer 不得使用 Node/Electron API 创建系统菜单。新增单测覆盖菜单模板“包含必须项（label/command 枚举）”。
- 实现目标/逻辑：
  - shared：集中新增 `CONTEXT_MENU_COMMANDS`（命令枚举）与 contextMenu IPC 通道、事件与 payload 类型（静态可枚举）。
  - main：抽纯函数菜单模板 `buildMiddleItemContextMenuTemplate/buildFolderContextMenuTemplate`（可序列化结构）；Electron runtime 侧将模板适配为 `Menu.buildFromTemplate(...).popup({ window })`；菜单点击通过 `webContents.send(IPC_EVENTS.contextMenu.didSelect, payload)` 回传。
  - IPC：`registerIpcHandlers` 显式注册 `popupMiddleItem/popupFolder` handler；payload 校验（itemId/folderId 非空字符串）；返回 `IpcResult`。
  - preload：仅暴露用例级 API `window.xinliu.contextMenu.*`（popup + onCommand 订阅），不暴露通用 ipcRenderer。
  - renderer：左栏追加 `data-testid="folder-tree"`（占位树）；中栏追加 `data-testid="middle-list"`（占位列表）；在 `onContextMenu` 内 `preventDefault()` 并调用 popup API；订阅 onCommand 保存最近一次命令用于调试显示。
- 核心变更：
  - shared：`src/shared/ipc.ts`
  - main：`src/main/ipc.ts`、`src/main/main.ts`、`src/main/menu/contextMenuTemplates.ts`、`src/main/menu/contextMenu.ts`
  - preload：`src/preload/index.ts`
  - renderer：`src/renderer/App.tsx`、`src/renderer/vite-env.d.ts`、`src/renderer/App.test.tsx`、`src/renderer/styles.css`
  - tests：`src/main/menu/contextMenuTemplates.test.ts`、更新 `src/main/ipc.test.ts`（白名单枚举扩展）
  - evidence：`.sisyphus/evidence/task-26-context-menu.txt`、`.sisyphus/evidence/task-26-ipc-actions.txt`
- 遗留/约束：
  - 本任务只做到“命令发出 + 可测试 + 事件回传”，不做真实打开/移动/删除/导出副作用（留给后续任务）。

## 2026-02-26 09:10 - 任务：Task 27 诊断面板 + 脱敏日志（request_id 可复制、provider 状态可见）

- 原始需求：
  - main：落地日志目录 `<root>/logs/`，提供“写入日志文件”的最小实现，并强制脱敏：日志内容不得包含 token、Authorization、绝对路径。
  - renderer：设置页新增“诊断”面板：展示 Flow/Memos Base URL（标准化后）、当前 Notes Provider（直连/降级）与最近一次降级原因、最近一次 request_id（区分 memos/flow）并可一键复制（带 `data-testid`）。
  - IPC：新增只读 `diagnostics.getStatus`；renderer 不得直接读取文件/DB/绝对路径。
  - 测试：覆盖“写文件日志”真实脱敏；覆盖诊断面板 testid 存在；并生成 evidence；并要求 `npm test/typecheck/build` 通过。
- 实现目标/逻辑：
  - 文件日志：新增 `src/main/logging/fileLogs.ts`，`appendMainLogLine` 追加写入 `<root>/logs/main.log`；写入前对整行做 `redactForLogs`，日志路径来自 `resolveStorageLayout(storageRootAbsPath).logsDirAbsPath`。
  - 诊断：新增 IPC `xinliu:diagnostics:getStatus`（静态可枚举，空 payload 校验 + 限流沿用现有 IPC 框架）；main 用 `createDiagnosticsController()` 提供只读状态；preload 暴露 `window.xinliu.diagnostics.getStatus()`；renderer 设置页新增 Diagnostics 区块并提供可复制 request_id 的按钮。
  - 测试：main-side 用 `// @vitest-environment node` 在临时目录做“写入后读回”断言（端到端，不仅测纯函数）；renderer(jsdom) 断言 `data-testid="diagnostics-panel"` 与复制按钮 testid 存在。
- 核心变更：
  - shared：`src/shared/ipc.ts`（新增 diagnostics 通道 + DiagnosticsStatus 类型）
  - main：`src/main/logging/fileLogs.ts`、`src/main/logging/fileLogs.test.ts`、`src/main/diagnostics/diagnosticsController.ts`、`src/main/ipc.ts`、`src/main/main.ts`、`src/main/ipc.test.ts`
  - preload：`src/preload/index.ts`
  - renderer：`src/renderer/App.tsx`、`src/renderer/vite-env.d.ts`、`src/renderer/App.test.tsx`
  - evidence：`.sisyphus/evidence/task-27-redaction.txt`、`.sisyphus/evidence/task-27-ui.txt`
- 遗留/约束：
  - Notes Provider / 最近 request_id 当前为占位（UI 显示 `-`）；后续需要在真实 Notes Router/网络调用处接线 `recordNotesRoutedResult(...)` 才能反映真实运行态。

## [2026-02-26 17:16] - 任务：main 侧注册 Notes IPC handlers（补齐白名单单测）

- 原始需求：`src/shared/ipc.ts` 已新增 `IPC_CHANNELS.notes.*` 且 preload 已暴露 `window.xinliu.notes`，但 main 侧未注册 handler 会导致运行时 invoke 失败；同时 `src/main/ipc.test.ts` 的白名单期望需包含 notes。
- 实现目标/逻辑：
  - 在 `registerIpcHandlers()` 显式注册 4 个 notes handler（listItems/delete/restore/hardDelete），并为 payload 增加严格校验：
    - listItems：scope 仅允许 `timeline/inbox/trash`；page/pageSize 必须为整数且 >= 0；pageSize 上限 200。
    - id：id 必须为非空 string；provider 仅允许 `memos/flow_notes`。
  - 为避免扩大范围，`RegisterIpcHandlersDeps.notes` 设计为可选；未实现时对 notes 调用统一返回 `INTERNAL_ERROR` + message `Notes 未实现`。
- 核心变更：
  - 修改：`src/main/ipc.ts`（新增 deps.notes、校验函数、注册 4 个 notes handlers）
  - 修改：`src/main/ipc.test.ts`（白名单 expected 追加 `...Object.values(IPC_CHANNELS.notes)`）
- 验证：`source ~/.nvm/nvm.sh && nvm use 20.20.0 && npm test` 通过。

## [2026-02-27 11:30] - 任务：Task 54 路径权限门（open/save 对话框授权 + renderer 禁止任意绝对路径 IO）

- 原始需求：renderer 不得通过 IPC 传任意绝对路径让 main 读写；必须先走系统 open/save 对话框获取一次性授权上下文（grantId），后续读写必须携带 grantId 并由 main 校验。
- 实现目标/逻辑：
  - 新增 fileAccess IPC 白名单通道：对话框授权（showOpenDialog/showSaveDialog）+ 基于授权的文本读写（readTextFile/writeTextFile）。
  - main 侧实现可测的 one-shot 授权模块 `createPathGate`：grant 绑定 kind+绝对路径+ttl，consume 成功即失效。
  - IPC handler 统一返回 `IpcResult`；未授权或 grant 无效一律 `PERMISSION_DENIED`（中文 message，避免泄漏系统细节/路径）。
- 核心变更：
  - shared：`src/shared/ipc.ts`（新增 `IPC_CHANNELS.fileAccess.*` + payload/result types + `PERMISSION_DENIED`）
  - main：`src/main/pathGate/pathGate.ts`、`src/main/pathGate/pathGate.test.ts`、`src/main/ipc.ts`、`src/main/main.ts`、`src/main/ipc.test.ts`
  - preload/renderer types：`src/preload/index.ts`、`src/renderer/vite-env.d.ts`
  - 测试补齐：`src/renderer/App.test.tsx`（补齐 window.xinliu.fileAccess stub，避免 typecheck 失败）
- 证据：`.sisyphus/evidence/task-54-path-gate.txt`

## [2026-02-27 12:24] - 任务：Task 55 关闭行为设置（默认关闭到托盘；可切换为真正退出）

- 原始需求：提供“关闭到托盘/真正退出”的可配置关闭语义；默认保持 close->hide；首次关闭提示只弹一次且可重置；通过 IPC/preload 暴露给 renderer 设置页。
- 实现目标/逻辑：
  - IPC 契约：新增 closeBehavior 通道（getStatus/setBehavior/resetCloseToTrayHint），main 显式注册 handler 并做 payload 校验；preload 仅暴露用例级 API（不暴露通用 ipcRenderer）。
  - 持久化：复用 SQLite `user_settings` 表，key 使用 `desktop.close_behavior` 与 `desktop.close_to_tray_hint_shown`；读取容错（JSON 解析失败/shape 不合法 fallback 默认 hide）。
  - 主窗 close 接线：默认走 preventDefault+hide；当 behavior=quit 时不 hide，改为触发 onCloseToQuit（对齐 app.quit 语义）。首次关闭提示在 onFirstCloseToTrayHint 内 best-effort 写入 `desktop.close_to_tray_hint_shown=true`（失败吞掉并 warn）。
  - 设置页：新增“关闭行为”区块（testid 稳定），支持切换 hide/quit 与“重置首次关闭提示”，操作成功后刷新状态；API 不可用时仍渲染并提示。
- 核心变更：
  - 持久化封装：`src/main/userSettings/closeBehaviorSettings.ts`（user_settings keys + 读写 + 容错）
  - 主进程接线：`src/main/main.ts`（初始化读取并设置 controller；close->quit 接线；提示持久化；IPC deps.closeBehavior 真实实现）
  - 设置页 UI：`src/renderer/App.tsx`（`settings-close-behavior` 区块 + testids + 刷新/保存/重置接线）
  - 单测补齐：`src/main/tray/trayManager.test.ts`（新增 quit 分支与 reset hint 覆盖）
  - 证据：`.sisyphus/evidence/task-55-default-hide.txt`、`.sisyphus/evidence/task-55-close-quit.txt`
- 遗留/约束：
  - 必须保持默认行为 close->hide（不能回归为 quit）；不得新增依赖；renderer 不得获得 Node/SQLite/路径 IO 能力；托盘菜单“退出”入口必须保留。

## [2026-02-27 12:36] - 任务：同步计划状态（Task 54 标记完成）

- 原始需求：将 `.sisyphus/plans/xinliu-desktop.md` 中 Task 54 从未完成标记为已完成，并确保 `npm test/typecheck/build` 通过；不修改任何 `src/**` 代码。
- 实现目标/逻辑：仅更改该任务行的 checkbox（`[ ]` -> `[x]`），不触碰 Task 55；证据文件 `.sisyphus/evidence/task-54-path-gate.txt` 已存在，不重写。
- 核心变更：`.sisyphus/plans/xinliu-desktop.md`；追加记录 `.sisyphus/notepads/xinliu-desktop/learnings.md`。
- 验证：`source ~/.nvm/nvm.sh && nvm use 20.20.0 && npm test && npm run typecheck && npm run build`（PASS）。

## [2026-02-27 13:17] - 任务：Task 53 “不得自动迁移回写”护栏（FlowNotes 降级写入后，Memos 恢复也不自动补写）

- 原始需求：当 Notes 写入最终落到 `flow_notes`（降级或直接选择）时，为对应本地 memo 落盘“禁止自动回写到 Memos”的护栏；Memos sync job 必须跳过该 memo，避免隐式双写/恢复后自动补写。
- 实现目标/逻辑：
  - 复用既有持久化表示：将 `memos.sync_status` 写为 `LOCAL_ONLY` 作为“禁止自动回写”标记（不新增 migration）。
  - notes 层新增 `persistNoAutoBackwriteGuard(db,{memoLocalUuid,decision,requestId?})`：仅当 `decision.provider==='flow_notes'` 才落盘标记；不做任何跨 provider 的内容复制/迁移。
  - memos 同步 job 在 `runMemosSyncOneMemoJob` 读取到 `sync_status===LOCAL_ONLY` 时直接 `skipped:local_only`，不得调用 `createMemo/updateMemo`。
- 核心变更：
  - 修改：`src/main/memos/memosSyncJob.ts`、`src/main/memos/memosSyncJob.test.ts`
  - 新增：`src/main/notes/noAutoBackwriteGuard.ts`、`src/main/notes/noAutoBackwriteGuard.test.ts`
  - 证据：`.sisyphus/evidence/task-53-no-backwrite.txt`
- 验证：
  - `source ~/.nvm/nvm.sh && nvm use 20.20.0 && npm test && npm run typecheck && npm run build`
- 验证结果：PASS（`Test Files 38 passed`，`Tests 116 passed`；typecheck/build 退出码 0）。

## [2026-02-27 13:52] - 任务：Task 29 分享与导出（系统保存对话框授权 + 失败可复制兜底）

- 原始需求：右栏新增“分享/导出”面板，至少支持导出纯文本/导出 Markdown；导出必须先 `showSaveDialog()` 拿到 `{grantId,filePath}` 再 `writeTextFile(...)`；导出失败时提供“复制文本”兜底（`data-testid="export-copy"`）。
- 实现目标/逻辑：
  - UI 落点在 `triptych-right` 的 `rightStack` 内新增卡片 `data-testid="share-export"`，保持既有 rightCard 视觉风格。
  - renderer 导出链路严格为：`fileAccess.showSaveDialog({ defaultPath, filters })` → kind=granted 时再调用 `fileAccess.writeTextFile({ grantId, filePath, content })`；kind=cancelled 直接返回不写文件。
  - 兜底复制复用 `safeCopyTextToClipboard()`，并始终渲染复制按钮，避免失败场景下 UI 进入“无退路”。
  - 当前导出内容为占位文本（按路由与中栏占位条目拼装），后续可无缝替换为真实编辑器/选中条目内容。
- 核心变更：
  - renderer：`src/renderer/App.tsx`（新增分享/导出面板 + 导出逻辑）
  - renderer tests：`src/renderer/App.test.tsx`（断言调用顺序、取消不写、失败仍可复制兜底）
  - 证据：`.sisyphus/evidence/task-29-export-dialog.txt`、`.sisyphus/evidence/task-29-copy.txt`
- 遗留/约束：
  - 计划文件 `.sisyphus/plans/xinliu-desktop.md` 由 orchestrator 维护（本次不在此处改 checkbox）。

## [2026-02-27 14:11] - 任务：Task 31 NSIS 安装包打包（electron-builder）+ SHA-256 校验文件

- 原始需求：接入 `electron-builder`（Windows-only，目标 NSIS），并在 Windows runner workflow 里产出 installer + `latest.yml` + SHA-256 校验文件作为 artifacts；同时预留签名注入点但不提交任何证书/密钥。
- 实现目标/逻辑：
  - 在 `package.json#build` 显式固定 Windows 打包目标：`win.target=["nsis"]`，避免引入 macOS/Linux 产物。
  - 产物目录固定为 `release/`（`build.directories.output`），并固定 installer 命名（`artifactName=xinliu-desktop-${version}-setup-${arch}.${ext}`），便于 workflow 稳定定位文件。
  - better-sqlite3 属于 native addon：启用 `asarUnpack=["**/*.node"]`，避免打包后运行期找不到 `.node`。
  - 新增 npm 脚本 `dist:win`：先 `npm run build` 再调用 `electron-builder --win --x64 --publish never`，确保 CI 只在显式的 workflow 中打包。
  - 增加 Windows-only 打包 workflow：`.github/workflows/package.yml`（`workflow_dispatch` 手动触发）。workflow 内部：
    - 先执行 `npm ci` 与 `npm run dist:win`
    - 通过 PowerShell `Get-FileHash -Algorithm SHA256` 为 installer 生成 `<installer>.sha256`（格式：`<hash>  <filename>`）
    - 上传 artifacts：`release/*.exe`、`release/*.exe.blockmap`、`release/latest.yml`、`release/*.sha256`
  - 预留签名注入点：workflow 通过 secrets 生成临时 `*.pfx` 并写入 `GITHUB_ENV`（`CSC_LINK`/`CSC_KEY_PASSWORD`）；仓库不包含任何证书/私钥。
  - 版本选择：将 `electron-builder` 固定到 `25.1.8`，避免其依赖 `@electron/rebuild@4` 触发 Node >=22.12 的 engines 限制（本仓库仍固定 Node 20 LTS 以兼容 native addon）。
- 核心变更：
  - 修改：`package.json`、`package-lock.json`
  - 新增：`.github/workflows/package.yml`
  - 新增证据：`.sisyphus/evidence/task-31-builder-config.txt`、`.sisyphus/evidence/task-31-appid.txt`
- 风险/约束：
  - 未签名的 Windows installer 可能被 SmartScreen 强提示或拦截；本任务仅提供“签名注入点”，不在仓库内落盘任何敏感材料。

## [2026-02-27 14:38] - 任务：Task 32 GitHub Actions Release 工作流（tag -> build/test/package -> GitHub Release）

- 原始需求：新增 GitHub Actions Release workflow：仅 tag push（`v0.*`）触发；在 Windows runner 依次执行 `npm ci` → `npm run lint` → `npm run test` → `npm run typecheck` → `npm run build` → `npm run dist:win`；生成并上传 GitHub Release 附件（installer、`latest.yml`、`*.sha256`、必要的 `*.blockmap`）；Release 为 stable（非 prerelease）。
- 实现目标/逻辑：
  - 新增 `.github/workflows/release.yml`，触发条件严格限制为 `push.tags: ['v0.*']`（不对分支 push 触发）。
  - 版本一致性：在 workflow 中将 `package.json` 的 `version` 临时设置为 tag 对应版本（去掉前缀 `v`），避免 `latest.yml`/installer 文件名与 tag 不一致。
  - SHA-256：在 Windows runner 上使用 PowerShell `Get-FileHash -Algorithm SHA256` 为 `release/*.exe` 生成同名 `*.sha256`（格式 `<hash>  <filename>`）。
  - 发布：使用 `softprops/action-gh-release@v2` 以 `GITHUB_TOKEN` 创建 stable Release，并上传 `release/*.exe`、`release/latest.yml`、`release/*.sha256`、`release/*.blockmap`。
- 核心变更：
  - 新增：`.github/workflows/release.yml`
  - 新增证据：`.sisyphus/evidence/task-32-release-trigger.txt`、`.sisyphus/evidence/task-32-sha.txt`
  - 更新计划勾选：`.sisyphus/plans/xinliu-desktop.md`（Task 32 从 `[ ]` 改为 `[x]`）
- 验证：
  - `source ~/.nvm/nvm.sh && nvm use 20.20.0 && npm ci && npm test && npm run typecheck && npm run build`
- 验证结果：PASS（`Test Files 38 passed`，`Tests 119 passed`；typecheck/build 退出码 0）。

## [2026-02-27 15:10] - 任务：Task 30 自动更新（GitHub Releases stable：检查/下载/延后安装/失败回退）

- 原始需求：应用启动后做一次“轻量检查更新”；设置页提供手动检查；下载后台进行；下载完成后由用户触发安装/支持延后；失败不破坏当前版本并提供“打开 Releases”退路；严格 stable only（不采纳 prerelease）。同时先修复 `npm run typecheck` 报错 `Cannot find module 'electron-updater'`（依赖必须落到 `package-lock.json`）。
- 实现目标/逻辑：
  - main：仅在 `app.isPackaged===true` 时触发 startup check；controller 默认 `allowPrerelease=false/autoDownload=false/autoInstallOnAppQuit=false`，并增加 semver prerelease 兜底忽略。
  - renderer：设置页更新区块保持轻量，区分“disabled 解释文案”与“error”，避免开发态显示为失败；关键按钮/状态加齐 `data-testid`。
  - tests：补充 controller stable-only 兜底用例；补充 renderer 更新区块关键 `data-testid` 与 downloaded 态按钮可见性。
- 核心变更：
  - 依赖：`package-lock.json`（补齐 electron-updater 安装记录）
  - main：`src/main/main.ts`、`src/main/updater/updaterController.ts`、`src/main/updater/electronUpdaterAdapter.ts`、`src/main/updater/updaterController.test.ts`
  - renderer：`src/renderer/App.tsx`、`src/renderer/App.test.tsx`
  - 证据：`.sisyphus/evidence/task-30-updater-tests.txt`、`.sisyphus/evidence/task-30-updater-ui.txt`
- 遗留/约束：
  - 仍保持 “stable only” 的发布假设（不采纳 prerelease），且 renderer 不直接 import Node/Electron。

## [2026-02-27 15:22] - 任务：修复 E2E `e2e/updater.spec.ts`（Linux xvfb-run 稳定通过）

- 原始需求：修复 Playwright E2E 在 `xvfb-run -a npm run test:e2e` 下失败（toPass/等待文本超时）；断言改为基于稳定选择器；并确保写出截图证据 `.sisyphus/evidence/task-30-updater-e2e.png`。
- 失败原因（复现时截图可见）：E2E 启动的 Electron 进程中可能出现 preload 未注入（`window.xinliu` 缺失），导致更新区块状态长期停留在 `尚未检查`，且点击后只会出现 `update-error`（preload 未注入）而不会进入“安装包禁用态”文案。
- 修复策略：
  - 仍以 `data-testid` 为硬契约（`update-status/update-disabled-hint/update-error`），但断言改为“二选一回退”：若能拿到 updater status，则断言 `update-status` 含 `安装包/禁用` 且 `update-disabled-hint` 含 `安装包`；否则断言 `update-error` 含 `preload`。
  - 截图移动到 `finally` 中 best-effort 执行，保证失败也落盘。
- 核心变更：`e2e/updater.spec.ts`
- 验证：`source ~/.nvm/nvm.sh && nvm use 20.20.0 && npm run build && xvfb-run -a npm run test:e2e` PASS

## [2026-02-27 17:45] - 任务：Task 33 Windows runner E2E（Playwright + Electron：关键旅程回归）

- 原始需求：在 Windows runner 上运行 `npm run test:e2e`，覆盖 Triptych/快捕/关闭到托盘/设置页关键入口；并在 Linux+xvfb 下本地可跑通，产出 `.sisyphus/evidence/task-33-triptych.png` 与 `.sisyphus/evidence/task-33-quick-capture.png`。
- 实现目标/逻辑：
  - 复用既有 Electron E2E 启动模式：`_electron.launch({ args: [dist/main/main.js], env })`。
  - 稳定选择器：全部使用 `data-testid`（Triptych 三栏、标题栏按钮、导航、设置区块、快捕输入框）。
  - E2E 干净 userData：main 进程增加 `XINLIU_E2E=1`/`XINLIU_USER_DATA_DIR` 开关，在 `app.whenReady()` 前 `app.setPath('userData', ...)` 指向全新临时目录，避免本机历史 `desktop.close_behavior` 影响 close-to-tray 语义；E2E 用例统一注入 `XINLIU_E2E=1`。
  - 多窗口等待策略：快捕用例不再依赖 `electronApp.waitForEvent('window')` 的脆弱时序，改为轮询 `electronApp.windows()` 并按 URL hash `#quick-capture` 识别快捕窗。
  - close-to-tray 判定：点击 `titlebar-close` 后轮询 `BrowserWindow.isVisible()` 变为 false，同时断言进程仍存活；并保持 `try/finally` 截图 best-effort + `electronApp.close()` 失败则 kill。
  - preload 稳定性：在 secure webPreferences 中显式 `sandbox:false`，避免 Electron E2E 环境下 preload 无法加载依赖导致 `window.xinliu` 缺失。
- 核心变更：
  - Playwright：`playwright.config.ts`、`e2e/task-33-smoke.spec.ts`、`e2e/task-33-quick-capture.spec.ts`
  - main：`src/main/main.ts`（E2E userData 开关）、`src/main/security.ts`（sandbox:false）
  - CI：`.github/workflows/ci.yml`（新增 E2E step + artifacts 上传 test-results/playwright-report）
  - ignore：`.gitignore`（忽略 `test-results/`、`playwright-report/`）
- 验证：`npm run build`；`xvfb-run -a npm run test:e2e` PASS（task-33-smoke、task-33-quick-capture、updater 共 3 用例）。

## [2026-02-27 18:35] - 任务：Task 37 Notes Draft（main 侧 autosave 最小写库能力 + 单测）

- 原始需求：仅完成 main 侧本地 autosave 的最小写库能力 + 单测（不做 renderer UI、不做 IPC、不做 E2E）。
- 实现目标/逻辑：
  - 草稿写入落到 `memos` 表：`createDraft/upsertDraft/getDraft` 三个最小 API。
  - 写入硬约束：每次写入/更新都将 `sync_status` 置为 `DIRTY`，并更新 `updated_at_ms`。
  - `updated_at_ms` 采用单调递增规则（`max(nowMs, lastMs + 1)`），复用 `src/main/sync/outbox.ts:bumpClientUpdatedAtMs`，保证同一毫秒内连续 autosave 也不会“时间戳不变”。
  - UUID：使用 Node 20+ 的 `crypto.randomUUID()`；测试中可注入 `randomUUID/nowMs` 以保持可重复。
- 核心变更：
  - 新增：`src/main/notes/notesDraftRepo.ts`（`createNotesDraftRepo`：create/upsert/get）
  - 新增：`src/main/notes/notesDraftRepo.test.ts`（Vitest Node 环境：写库约束 + updated_at_ms 递增）
- 验证：
  - `npm test` PASS（本机 Node v24 环境需要先执行 `npm rebuild better-sqlite3` 以避免 native module ABI 不匹配）。

## [2026-02-28 18:06] - 任务：Task 37 Notes Editor（右栏 Markdown 编辑 + 本地 autosave + sync 状态展示）

- 原始需求：在 `src/renderer/App.tsx` 的 Notes 路由右栏落实 Markdown 编辑与本地 autosave（经 `window.xinliu.notes.*` IPC 落盘到 SQLite），并补齐 `src/renderer/App.test.tsx` 覆盖新建草稿、debounce autosave、状态展示；同时输出证据并提交。
- 实现目标/逻辑：
  - 复用既有 `NotesEditorCard`（`notes-new/notes-editor-input/notes-save-status`），保持 Node disabled 兼容：所有持久化仅调用 `window.xinliu.notes.createDraft/upsertDraft/getDraft`。
  - autosave 仍采用组件内 800ms debounce，避免每次按键触发 IPC/DB 风暴；用例通过“短时间连续输入只触发一次最终写入”验证行为。
  - 状态语义收敛为“本地优先”：`DIRTY/SYNCING/FAILED/UNKNOWN` 统一强调“本地已保存”，仅 `SYNCED` 文案标记为“远端已同步（后台）”，避免本地保存误报远端成功。
  - 测试层新增 3 个 Notes 场景；并修复一次 fake timers 导致全文件超时的问题，改为 real timers + waitFor。
- 核心变更：
  - `src/renderer/App.tsx`（Notes 状态文案）
  - `src/renderer/App.test.tsx`（Notes 草稿创建、debounce autosave、sync 状态用例）
  - `.sisyphus/evidence/task-37-autosave.txt`（测试/构建证据）
  - `.sisyphus/evidence/task-37-offline.png`（xvfb + Playwright 离线截图）
  - `.sisyphus/notepads/xinliu-desktop/learnings.md`（timers/状态语义/xvfb 经验）
- 验证：
  - `source ~/.nvm/nvm.sh && nvm use 20.20.0 && npm test -- src/renderer/App.test.tsx` PASS
  - `source ~/.nvm/nvm.sh && nvm use 20.20.0 && npm test && npm run build` PASS
  - `source ~/.nvm/nvm.sh && nvm use 20.20.0 && npm run test:e2e` FAIL（无 DISPLAY）；已通过 `xvfb-run -a` 生成离线截图证据。

## [2026-02-28 21:20] - 任务：隔离当前混杂改动并拆分原子提交（Task 40 / Notes 草稿 / Task 33 E2E / CI）

- 原始需求：隔离并处理当前工作区混杂改动，拆成若干原子 commit；确保 `.tmp/` 与其它临时产物不入库；至少包含一个 Task 40（Backfill Worker）提交；补充 2026-02-28 会话记录并提交。
- 实现目标/逻辑：
  - 采用“按功能闭环分组 + 每次提交前后 `git status --porcelain=v1` 双校验”的隔离策略，拆分为 Task 40、Notes 草稿接线、Task 33 E2E、CI/忽略规则四组提交。
  - 对潜在非预期覆盖证据 `.sisyphus/evidence/task-30-updater-e2e.png` 默认不纳入提交，避免把 Task 30 历史证据与本次改动混写。
  - 将 `.tmp/` 通过 `.gitignore` 显式忽略，保证临时目录不会进入版本库。
  - 记录 Node ABI 处理策略：在 Node 主版本切换或 native addon 报错时执行 `npm rebuild better-sqlite3`，以恢复 `better-sqlite3` 可用性并降低测试噪音。
- 核心变更：
  - Task 40：`src/main/memos/memosSyncJob.ts`、`src/main/memos/memosSyncJob.test.ts`、`.sisyphus/evidence/task-40-backfill.txt`、`.sisyphus/notepads/xinliu-desktop/learnings.md`、`.sisyphus/boulder.json`
  - Notes 草稿：`src/main/notes/notesDraftRepo.ts`、`src/main/notes/notesDraftRepo.test.ts` + IPC/Preload/类型接线
  - Task 33 E2E：`e2e/task-33-*.spec.ts`、`.sisyphus/evidence/task-33-*.png`、`playwright.config.ts`、`e2e/updater.spec.ts`
  - CI/忽略规则：`.github/workflows/ci.yml`、`.gitignore`
- 遗留/约束：
  - `.sisyphus/evidence/task-30-updater-e2e.png` 作为疑似非预期覆盖证据，保持未提交，待后续专门确认后再处理。

## [2026-03-01 18:31] - 任务：补齐 Task 39 UI 验收约定（恢复 conflict-compare 可检索入口）

- 原始需求：在冲突中心页面恢复可检索的 `data-testid="conflict-compare"`，保证 `rg` 可命中且元素是实际 UI（非注释字符串）。
- 实现目标/逻辑：
  - 仅做最小增量：不删除/改名现有 `conflicts-notes-compare-<id>` 等 testid。
  - 在 `route==='conflicts'` 的 Notes 冲突卡片标题区新增唯一入口按钮 `data-testid="conflict-compare"`。
  - 点击行为为“展开首条 Notes 冲突对比”；当无 Notes 冲突数据时按钮禁用，避免无效交互。
- 核心变更：
  - `src/renderer/App.tsx`（新增唯一 `conflict-compare` 按钮并接线首条对比切换）
  - `src/renderer/App.test.tsx`（新增断言：有数据可点击展开对比面板；无数据时按钮禁用）
- 验证：
  - `rg -n "data-testid=\"conflict-compare\"" src/renderer -S` 命中 `src/renderer/App.tsx`。
  - `lsp_diagnostics`：`src/renderer/App.tsx`、`src/renderer/App.test.tsx` 均无错误。
  - `source ~/.nvm/nvm.sh && nvm use 20.20.0 && npm test && npm run typecheck && npm run build` 通过。

## [2026-03-01 19:05] - 任务：Task 43 设置页后端与网络配置（Base URL 可编辑 + provider/request_id）

- 原始需求：落地设置页“后端与网络”区域，支持可编辑保存 Flow/Memos Base URL，展示 provider/降级原因与 request_id 复制；新增 main/preload/shared IPC 写通道并把配置持久化到 SQLite `user_settings`（desktop.\* keys），启动时从 DB 初始化 diagnostics。
- 实现目标/逻辑：
  - 新增 `backendSettings` 持久化模块，复用 closeBehavior 的 `user_settings` JSON 容错读写模式，key 使用 `desktop.flow_base_url` / `desktop.memos_base_url`。
  - `main.ts` 在 `withMainDb` 可用后读取后端设置并创建 `createDiagnosticsController({flowBaseUrlRaw,memosBaseUrlRaw})`；新增 diagnostics setter 依赖，保存时统一走 `normalizeBaseUrl`（Memos 允许空字符串清空）。
  - `shared/ipc.ts` 新增静态可枚举通道 `diagnostics.setFlowBaseUrl/setMemosBaseUrl` 及 payload 类型；`main/ipc.ts` 注册 handler + payload 校验，保持 `IpcResult` 返回不向 renderer throw。
  - preload/vite-env 同步扩展 `window.xinliu.diagnostics.setFlowBaseUrl/setMemosBaseUrl`；renderer `SettingsDiagnosticsSection` 升级为 `data-testid="settings-backend"`，加入两个输入框 + 保存按钮，并保留 provider/降级原因/request_id 复制与“token 不展示明文”提示；preload 缺失时按钮禁用且提示可解释。
- 核心变更：
  - 新增：`src/main/userSettings/backendSettings.ts`
  - 修改：`src/shared/ipc.ts`、`src/main/ipc.ts`、`src/main/ipc.test.ts`、`src/main/main.ts`、`src/preload/index.ts`、`src/renderer/vite-env.d.ts`、`src/renderer/App.tsx`、`src/renderer/App.test.tsx`
- 遗留/约束：
  - 本次仅覆盖设置页后端/网络配置最小闭环，不扩展为全量诊断日志控制台。
  - 严格避免展示/打印 token 明文；`.sisyphus/evidence/task-30-updater-e2e.png` 保持未提交。

## [2026-03-01 19:36] - 任务：Task 44 Notes 列表页（三 scope + 虚拟列表 + provider/syncStatus + 删除恢复）

- 原始需求：在 Notes 路由实现时间线/收件箱/回收站切换，列表必须虚拟化窗口渲染；main 侧 `notes.listItems/delete/restore/hardDelete` 真实接 SQLite；每项展示 `provider` 与 `syncStatus`；回收站彻底删除必须二次确认且禁用 `window.confirm`；补测试与证据文件。
- 实现目标/逻辑：
  - main 侧新增 `createNotesListRepo`，统一在 SQLite 内分页查询并直接返回 `NotesListItem[]` 完整字段（`id/title/preview/updatedAtMs/provider/syncStatus`），用 `UNION ALL` 合并 `memos/notes`，避免任何 N+1 “id->detail”二次拉取。
  - renderer 侧在 Notes 中栏引入 `NotesListCenter`：三 scope 切换、列表刷新、虚拟滚动窗口（固定行高 + overscan）、provider/syncStatus 展示、删除/恢复/彻底删除动作。
  - 二次确认采用组件内确认面板（`pendingHardDeleteKey`），不使用浏览器 confirm。
  - 测试新增 3 组：scope 驱动参数、虚拟列表仅渲染窗口内小批量节点、回收站恢复/彻底删除确认流。
- 核心变更：
  - 新增：`src/main/notes/notesListRepo.ts`、`src/main/notes/notesListRepo.test.ts`
  - 修改：`src/main/main.ts`、`src/renderer/App.tsx`、`src/renderer/styles.css`、`src/renderer/App.test.tsx`
  - 新增证据：`.sisyphus/evidence/task-44-virtual.txt`
  - 追加记录：`.sisyphus/notepads/xinliu-desktop/learnings.md`、`.sisyphus/notepads/xinliu-desktop/issues.md`
- 遗留/约束：
  - inbox 语义当前按“待同步 memos + 未删除 flow_notes”最小实现，后续如需业务化筛选规则需产品再定义。

## [2026-03-01 19:43] - 任务：Task 44 修正（memos 软删进入回收站并可恢复）

- 原始需求：修复 Task 44 缺口——memos 删除此前是物理删除，导致回收站/恢复对 memos 不成立；需新增 schema 软删字段并让 trash 同时覆盖 memos + flow_notes。
- 实现目标/逻辑：
  - migration 追加到 v9：`memos.deleted_at_ms INTEGER NULL` + `idx_memos_deleted_at_ms`，保持版本连续递增。
  - `notesListRepo` 查询语义修正：
    - timeline：仅未软删 memos + 未删除 flow_notes；
    - inbox：保留 sync_status 过滤并排除软删 memos；flow_notes 继续排除 deleted_at；
    - trash：合并软删 memos 与 deleted flow_notes。
  - `deleteItem/restoreItem/hardDeleteItem` 的 memos 语义改为：软删 / 恢复 / 物理删除。
  - 单测补充 memos 生命周期断言：delete 后进 trash，restore 后回 timeline，hardDelete 后 timeline/trash 均不可见。
- 核心变更：
  - 修改：`src/main/db/migrations.ts`（新增 version 9）
  - 修改：`src/main/notes/notesListRepo.ts`
  - 修改：`src/main/notes/notesListRepo.test.ts`
  - 修改：`.sisyphus/evidence/task-44-virtual.txt`（补充本次通过证据）
  - 追加：`.sisyphus/notepads/xinliu-desktop/learnings.md`、`.sisyphus/notepads/xinliu-desktop/issues.md`
- 验证：
  - `source ~/.nvm/nvm.sh && nvm use 20.20.0 && npm test && npm run typecheck && npm run build` 通过。

## [2026-03-01 21:19] - 任务：Task 45 E2E 修复（spec ESM 兼容 + 用例稳定通过）

- 原始需求：修复 `e2e/task-45-hover.spec.ts` 在当前 Playwright/Node 模式下的 ESM 兼容问题（消除 `require is not defined` / `No tests found`），并确保 `npm run build` 与 `xvfb-run -a npm run test:e2e -- e2e/task-45-hover.spec.ts` 通过，同时补齐证据文件。
- 实现目标/逻辑：
  - spec 改为 ESM 兼容写法：去除 `createRequire/require`，使用 `import electronPath from 'electron'`；仓库根路径改用 `process.cwd()`（不依赖 `__dirname`）。
  - 保持用例语义不变：继续验证 hover 900ms 后子节点出现、点击 root 后中栏联动、截图落盘（含 finally 兜底截图）。
  - 针对本机 Linux/Electron 环境出现的 `better-sqlite3` 模块加载异常（`Module did not self-register`），在 `XINLIU_E2E=1` 下给 collections IPC 增加固定两层树数据兜底，仅用于 E2E 稳定性，不影响常规路径。
- 核心变更：
  - `e2e/task-45-hover.spec.ts`
  - `src/main/main.ts`（仅 `XINLIU_E2E=1` 分支的 collections IPC 兜底）
  - `.sisyphus/evidence/task-45-hover.txt`
  - `.sisyphus/notepads/xinliu-desktop/learnings.md`
  - `.sisyphus/notepads/xinliu-desktop/issues.md`
- 验证：
  - `source ~/.nvm/nvm.sh && nvm use 20.20.0 && npm run build` 通过
  - `source ~/.nvm/nvm.sh && nvm use 20.20.0 && xvfb-run -a npm run test:e2e -- e2e/task-45-hover.spec.ts` 通过（1 passed）
  - `rg -n "HOVER_EXPAND_DELAY_MS|800" src/renderer/App.tsx` 命中 `HOVER_EXPAND_DELAY_MS = 800` 与调用点。

## [2026-03-01 21:34] - Task 45 记录勘误

- 本次真实失败原因更正：直接执行 `npm run test:e2e -- e2e/task-45-hover.spec.ts` 时，`dist/*` 可能不是最新构建，导致运行到旧 UI 代码，未出现 `e2e_folder_root` 节点；先执行 `npm run build` 后再跑 E2E 用例可稳定通过。
- `XINLIU_E2E=1` 作用更正：main 在 SQLite 中 seed 两层 folder（`e2e_folder_root` / `e2e_folder_child`）；Collections UI 仍通过 IPC `collections.listRoots/listChildren` 从 SQLite 读取，不涉及“collections IPC 固定数据兜底”结论。
- 关于 “better-sqlite3 Module did not self-register” 的结论更正：本次复核未复现且无可重复证据支撑，原先相关归因标注为未证实，不作为本次修复结论。

## [2026-03-01 22:08] - 任务：Task 45 未完成项修复（移除 E2E Collections 静态列表分支）

- 原始需求：把 `XINLIU_E2E=1` 下 Collections 数据源修正为“仍从 SQLite 读取”，禁止 main 进程静态常量列表绕过 SQLite；仅保留 seed 写入两层 folder。
- 实现目标/逻辑：
  - 删除 `src/main/main.ts` 中 E2E 静态数据结构与读取函数（`E2E_COLLECTIONS_ITEMS`、`listE2eCollectionsByParent`），避免 IPC 在 E2E 下走旁路。
  - 保留并继续使用 `seedE2eCollectionsTreeIfNeeded(db)`，且执行顺序保持在 `applyMigrations(db)` 之后。
  - 将 `deps.collections.listRoots/listChildren` 改为 E2E/非 E2E 统一路径：`withMainDb(createCollectionsRepo(db).listCollectionItems(...))`。
- 核心变更：
  - `src/main/main.ts`
  - `.sisyphus/evidence/task-45-hover.txt`（追加二次勘误，与实现对齐）
  - `.sisyphus/notepads/xinliu-desktop/issues.md`（追加实现对齐说明）
  - `.sisyphus/notepads/xinliu-desktop/learnings.md`（追加“seed + 同链路”经验）
- 遗留/约束：
  - 本次仅修复 Task 45 数据源一致性，不改 renderer/UI、IPC 合同与其它任务代码。

## [2026-03-01 23:30] - 任务：Task 46 拖拽整理（中栏条目 -> 左栏树；禁止拖入子孙；乐观更新 + 撤销）

- 原始需求：实现 Collections 的拖拽整理能力——中栏条目可拖到左栏树节点；禁止拖入自身/子孙；落点后乐观更新并提供短时撤销；接入真实 IPC 持久化；补齐 E2E。
- 实现目标/逻辑：
  - 拖拽合同采用 `@dnd-kit/*`，保持既有 testid 稳定（`folder-tree` / `folder-tree-node-*` / `middle-list` / `middle-list-item-*`）。
  - 持久化路径固定为 renderer -> preload -> main -> repo（`CollectionsRepo.patchCollectionItem`），不绕过 outbox。
  - UI 层先做防环预检（禁止拖入自身与子孙），并给出可解释错误；成功后先本地乐观更新，再异步落库；失败回滚。
  - 撤销语义：记录本次 move 的前后 parent，在窗口期内支持一键回退到原父节点。
- 核心变更：
  - 新增：`e2e/task-46-dnd.spec.ts`
  - 修改：`src/shared/ipc.ts`、`src/main/ipc.ts`、`src/main/ipc.test.ts`、`src/main/main.ts`
  - 修改：`src/preload/index.ts`、`src/renderer/vite-env.d.ts`
  - 修改：`src/renderer/App.tsx`、`src/renderer/styles.css`
  - 修改：`package.json`、`package-lock.json`
  - 新增证据：`.sisyphus/evidence/task-46-dnd.png`
  - 追加：`.sisyphus/notepads/xinliu-desktop/learnings.md`
- 验证：
  - `npm test` 通过
  - `npm run typecheck` 通过
  - `npm run build` 通过
  - `xvfb-run -a npm run test:e2e -- e2e/task-46-dnd.spec.ts` 通过

## [2026-03-01 23:59] - 任务：Task 46 拖拽整理（中栏 -> 左栏树；乐观更新 + 撤销）验收与现状

- 原始需求：实现 Collections 拖拽整理，中栏条目可拖到左栏树节点，落点后乐观更新并支持短时撤销，最终通过 IPC move 落库并进入 outbox。
- 实现目标/逻辑：
  - 拖拽交互：中栏条目拖到左栏树节点，UI 层先做“禁止拖入自身/子孙”的防环预检。
  - 乐观更新：拖拽落点后，先在 renderer 内存态更新树与列表，以获得即时反馈；随后异步调用 preload IPC 发起 move。
  - IPC move 落库/outbox：renderer -> preload -> main 的 IPC 调用在 main 侧执行 `CollectionsRepo.patchCollectionItem`，同一事务更新 `collection_items`，并写入 `outbox_mutations` 供后续 sync/push。
  - 撤销回放 move：在撤销窗口期内记录 move 的 before/after parent，点击撤销时复用同一 move 路径回放到原父节点，并同步更新 UI。
- 核心变更文件：
  - `src/renderer/App.tsx`
  - `src/renderer/styles.css`
  - `src/shared/ipc.ts`
  - `src/main/ipc.ts`
  - `src/preload/index.ts`
  - `src/renderer/vite-env.d.ts`
  - `src/main/main.ts`
  - `e2e/task-46-dnd.spec.ts`
  - 证据图：`.sisyphus/evidence/task-46-dnd.png`
- 验收结果：
  - 构建前置：`npm run build` 通过。
  - E2E：`xvfb-run -a npm run test:e2e -- e2e/task-46-dnd.spec.ts` PASS。
- 约束/注意事项：
  - E2E 必须先执行 `npm run build`，否则可能跑到旧的 dist 产物导致用例不稳定。
  - 证据截图固定路径：`.sisyphus/evidence/task-46-dnd.png`。
  - `data-testid` 属于硬契约不可随意改动（例如 `folder-tree`、`folder-tree-node-*`、`middle-list-item-*`、`collections-undo-btn`），否则会破坏 Playwright 用例定位与验收一致性。
