PLAN.md 关键合同摘录（SQLite 与 Storage Root）：
- 1.5.1：本地 SQLite 是离线权威源；SQLite 连接只在 main；写入必须事务化（业务表 + outbox 同事务）。
- 1.5.2：Storage Root 目录布局包含 <root>/db/（SQLite 主库 + WAL/SHM）。
- 1.3.4：更改 Storage Root 迁移至少包含 SQLite DB + WAL/SHM（如存在）。

本次实现（Task 8）：
- 引入 better-sqlite3（main-only），提供 open/close + PRAGMA（WAL/foreign_keys/synchronous/busy_timeout）。
- 采用 PRAGMA user_version 作为版本号，提供事务化迁移引擎 applyMigrations（迁移版本必须从 1 连续递增）。
- 单测在 Vitest Node 环境跑通（不依赖 Electron runtime）。

验证输出（Node 20）：

$ node -v
v20.20.0

$ npm run typecheck
(PASS)

$ npm test
Test Files  6 passed (6)
Tests       17 passed (17)

$ npm run build
(PASS)
