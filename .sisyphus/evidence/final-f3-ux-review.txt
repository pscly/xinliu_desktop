主流程 [PASS] | 异常流 [PASS] | 键盘优先 [PASS] | 臃肿风险点 [右栏职责过宽, 左栏全局入口可能堆叠, 托盘/全局入口重复暴露] | VERDICT: PASS

依据: 仅审阅 `PLAN.md`。

Triptych 一致性
- 左栏/中栏/右栏职责在 `1.2.1` 明确且可贯穿主视图集合(`1.2.3`)；并给出窄宽降级规则(`1.2.1`)。
- Folder/Collections 树被定义为结构层权威入口(`1.2.2`)，与“拖拽整理”的目标一致(`1.2.5-3`)。

关键旅程覆盖(主流程)
- 快捕: 全局快捷键 + 托盘菜单 + 应用内按钮兜底；Enter 保存回托盘、Esc 取消(`1.2.5-1`)。
- 搜索: 主窗搜索框 + 全局快捷键聚焦；中栏结果、右栏详情并支持“定位到 Folder”；索引不可用时可重建且降级(`1.2.5-2`)。
- 拖拽整理: hover 800ms 展开、禁止拖入子孙、edge scrolling、乐观更新+撤销(`1.2.4`,`1.2.5-3`)。
- 删除/恢复: 软删除到回收站、回收站恢复/彻底删除、彻底删除二次确认(`1.2.5-4`)。
- 分享/导出: 右栏提供面板、必须走系统保存对话框授权、失败提供复制文本兜底(`1.2.5-5`,`1.7.3`)。
- 冲突处理: Flow rejected/server_snapshot 可查看且提供保守恢复与高级强制覆盖；Memos 生成冲突副本保留本地文本并回滚原记录为服务端版本(`1.2.5-6`,`1.6.2`,`1.8.2`)。

异常流覆盖
- 全局快捷键注册失败: 设置页显式提示并引导改键，托盘入口仍可用(`1.2.5-1`,`1.3.3`)。
- 搜索索引不可用: 提示重建并降级、不阻断(`1.2.5-2`)。
- 分享/导出失败: 复制文本兜底(`1.2.5-5`)。
- Flow 同步: 以 applied/rejected 为准；429 遵守 Retry-After；401 停止重试引导重登；冲突证据持久化并可见(`1.6.1.3`,`1.8.2`)。
- Memos 同步: 本地编辑保护、不覆盖未同步编辑；冲突副本策略明确(`1.6.2`)。

键盘优先(最低关键快捷键与退路)
- 打开主窗/聚焦搜索: 通过“全局快捷键(打开主窗并聚焦搜索框)”定义(`1.2.5-2`)。
- 快捕: 全局快捷键入口 + 托盘入口；Enter 保存、Esc 取消，并回到托盘(`1.2.5-1`,`1.3.2`)。
- 关闭到托盘: 关闭按钮默认隐藏到托盘，退出仅托盘菜单(`1.3.2`)。
- 失败退路: 快捷键注册失败可见提示+改键；托盘与应用内入口兜底(`1.2.5-1`,`1.3.2`,`1.3.3`)。

残余风险/实现期注意点(<=3)
1) “保存”快捷键目前仅对快捕(Enter)明确；右栏编辑器的保存/撤销/关闭快捷键需要在实现期补齐，否则会弱化“键盘优先”的一致性。
2) 拖拽的“禁止拖入子孙”虽有硬约束，但缺少明确的失败反馈形态(光标/提示/震动/原因文案)；实现期需保证用户可理解且不误操作。
3) 右栏承载(详情/编辑/导出/冲突对比/网格)较多，易变“万能面板”；实现期建议强制定义每个模式的主动作/次动作/溢出入口，避免把所有功能堆在同一层级。
