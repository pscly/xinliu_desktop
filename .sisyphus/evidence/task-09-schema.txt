Task 9：Flow 领域表 Schema（Todo + Collections + tombstone）

合同来源：
- PLAN.md：要求 Flow 侧至少落库 `todo_lists`、`todo_items`、`todo_occurrences`、`collection_items`，且必须支持 tombstone `deleted_at`。
- apidocs/collections.zh-CN.md：明确 tombstone 语义：删除不物理删除，设置 `deleted_at`。

实现说明：
- 迁移版本 v2：创建 4 张表并包含 `deleted_at` 列；同时添加常用索引（按 parent_id / deleted_at / client_updated_at_ms 等）。
- 单测（Vitest Node 环境）：
  - 迁移后 sqlite_master 中存在上述 4 张表。
  - PRAGMA table_info(table) 验证 4 张表都包含 `deleted_at` 列。

验证输出：

$ npm test
Test Files  7 passed (7)
Tests       19 passed (19)
