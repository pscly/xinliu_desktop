Task 55 证据：默认关闭到托盘（preventDefault + hide）

场景说明：
- 默认关闭行为：点击窗口关闭按钮/触发 close 事件时，会 preventDefault 并 hide（关闭到托盘），不会真正退出。

对应单测名称：
- 窗口 close 事件：默认 preventDefault + hide（不退出）

验证命令：
- source ~/.nvm/nvm.sh && nvm use 20.20.0 && npm test -- src/main/tray/trayManager.test.ts

命令输出片段：
Now using node v20.20.0 (npm v10.8.2)
> vitest run src/main/tray/trayManager.test.ts
✓ src/main/tray/trayManager.test.ts (5 tests)
Test Files  1 passed (1)
Tests       5 passed (5)
